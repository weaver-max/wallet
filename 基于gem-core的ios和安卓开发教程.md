# 基于 gem-core 的 iOS / Android 开发教程

> 面向：需要理解或参与 Gem Wallet 跨端开发的工程师
> 内容：Rust Core 如何编译进两端、日常开发怎么跑、应用层怎么调用、网络/存储如何委托给平台、以及 Core 独立成仓后如何分发

---

## 目录

1. [全景：一句话讲清这套架构](#1-全景一句话讲清这套架构)
2. [两条管线：TypeShare 与 UniFFI](#2-两条管线typeshare-与-uniffi)
3. [Core 侧：接口是怎么定义的](#3-core-侧接口是怎么定义的)
4. [iOS 构建链路详解](#4-ios-构建链路详解)
5. [Android 构建链路详解](#5-android-构建链路详解)
6. [开发流程：从环境搭建到提交](#6-开发流程从环境搭建到提交)
7. [应用层怎么调用 Rust](#7-应用层怎么调用-rust)
8. [深入：网络请求为什么委托给平台，怎么做的](#8-深入网络请求为什么委托给平台怎么做的)
9. [进阶：Core 独立成仓后如何分发](#9-进阶core-独立成仓后如何分发)
10. [常见坑速查](#10-常见坑速查)
11. [命令速查表](#11-命令速查表)

---

## 1. 全景：一句话讲清这套架构

**所有业务逻辑用 Rust 写一遍，iOS 和 Android 只写 UI。**

```
                          ┌───────────────────────────────┐
        ②③ 主动调用 ─────► │    core/gemstone  (Rust)      │
                          │  钱包逻辑 / 签名 / 交易构造      │
        ④ 能力回调 ◄────── │  网络请求委托给平台             │
                          └───────────────────────────────┘
                                 ▲                  ▲
                                 │                  │
              Gemstone.swift             uniffi/*.kt
              + libgemstone.a            + libgemstone.so
              （手动生成）                 （Gradle 自动）
                                 │                  │
                          ┌──────┴──────┐    ┌──────┴──────┐
                          │  iOS 应用    │    │ Android 应用 │
                          │  SwiftUI    │    │  Compose    │
                          └─────────────┘    └─────────────┘

        ① Primitives.swift / primitives/*.kt  ← 纯数据模型，不过 FFI，入库
```

四种交互方式（第 7 章详解）：

| 编号 | 方式 | 有没有原生代码 |
|---|---|---|
| ① | TypeShare 数据模型 | ❌ 纯源码翻译 |
| ② | UniFFI 顶层函数 | ✅ |
| ③ | UniFFI 对象（有状态） | ✅ |
| ④ | UniFFI 反向回调（平台实现，Rust 调用） | ✅ |

---

## 2. 两条管线：TypeShare 与 UniFFI

这是**最容易混淆**的地方。仓库里有两套完全独立的代码生成，服务不同目的。

| | **TypeShare 管线** | **UniFFI 管线** |
|---|---|---|
| **来源** | `core/crates/primitives` | `core/gemstone` |
| **生成什么** | Swift `struct` / Kotlin `data class` | 绑定胶水 + 原生库 |
| **有原生代码吗** | ❌ 没有，纯源码翻译 | ✅ `.a` / `.so` |
| **运行时开销** | 零 | 跨 FFI 边界 |
| **产物入 git 吗** | ✅ 入库 | ❌ gitignore |
| **触发命令** | `just generate-models` | `just generate-stone` |
| **典型内容** | `Chain`、`AssetId`、`FiatQuoteType` | `Config`、`deeplinkBuildUrl`、`AlienProvider` |

一句话区分：**TypeShare 搬数据结构，UniFFI 搬行为。**

### TypeShare 细节

`core/bin/generate` 扫描 `core/crates/primitives/src/**`，逐文件调用 `typeshare` CLI：

```bash
cargo run -p generate --bin generate ios     ../ios/Packages
cargo run -p generate --bin generate android ../android/gemcore/src/main/kotlin/com/wallet/core
```

跳过 `lib.rs` / `client.rs` / `keccak.rs` 等实现文件（见 `main.rs` 里的 `ignored_files`）。类型映射写在 `core/typeshare.toml`：

```toml
[swift.type_mappings]
"BigInt"   = "String"      # Swift 没有 BigInt，用字符串传
"DateTime" = "Date"

[kotlin.type_mappings]
"UInt64" = "Long"          # Kotlin 无符号类型体验差，统一降级
"Value"  = "JsonValue"
```

产物：

- iOS → `ios/Packages/Primitives/Sources/Generated/*.swift`
- Android → `android/gemcore/src/main/kotlin/com/wallet/core/primitives/*.kt`

> 这些文件**入库**，头部带 `Generated by typeshare` 注释。**永远不要手改**。要扩展就写 extension，放在单独文件里。

同一个 `generate` 二进制还有个 `localize` 子命令，把 `localization/app/*.ftl` 转成 iOS `.strings` 和 Android `strings.xml`。

### UniFFI 细节

`core/gemstone/Cargo.toml` 一个 crate 同时产出三种形态：

```toml
[lib]
crate-type = [
    "staticlib",  # iOS    → libgemstone.a
    "rlib",       # 给其他 Rust crate 复用
    "cdylib",     # Android → libgemstone.so
]
```

`core/gemstone/src/lib.rs:34` 用 proc-macro 模式，**没有 `.udl` 文件**：

```rust
uniffi::setup_scaffolding!("gemstone");
```

接口靠 `#[uniffi::export]` / `#[derive(uniffi::Record)]` 直接标在 Rust 类型上，比维护单独的 IDL 文件省心得多。

`core/gemstone/uniffi.toml` 配置两端命名：

```toml
[bindings.kotlin]
cdylib_name = "gemstone"          # 运行时 System.loadLibrary 的名字

[bindings.swift]
ffi_module_name = "GemstoneFFI"   # 生成的 C module 名
generate_module_map = false       # monorepo 里靠 SPM 提供，跨仓库要改 true
experimental_sendable_value_types = true
```

**绑定生成的套路（两端相同）**，见 `core/gemstone/justfile`：

```bash
# 1. 先编一个宿主机动态库
cargo build                    # → core/target/debug/libgemstone.dylib

# 2. 从动态库里"反查"元数据生成绑定（--library 模式，不解析 UDL）
cargo run -p uniffi-bindgen -- generate \
    --library --language swift|kotlin --crate gemstone \
    -o generated/... \
    ../target/debug/libgemstone.dylib
```

`core/bin/uniffi-bindgen` 就是一行包装：

```rust
fn main() { uniffi::uniffi_bindgen_main() }
```

---

## 3. Core 侧：接口是怎么定义的

### 导出一个函数

```rust
// core/gemstone/src/deeplink.rs
#[uniffi::export]
pub fn deeplink_build_url(deeplink: Deeplink) -> String { ... }
```

Rust 的 `snake_case` 会自动转成 Swift/Kotlin 的 `lowerCamelCase` → `deeplinkBuildUrl`。

### 导出一个枚举

```rust
pub enum Deeplink {
    Asset { asset_id: String },
    Buy   { asset_id: String, amount: Option<f64> },
    Perpetuals,
}
```

到两端变成 sealed 类型，可以穷举匹配：

```kotlin
when (this) {
    is Deeplink.Asset -> ...
    is Deeplink.Buy   -> ...
    Deeplink.Perpetuals -> null
}
```

### 让平台实现 trait（反向回调）

```rust
// core/gemstone/src/alien/provider.rs:8
#[uniffi::export(with_foreign)]   // with_foreign = 外部语言可以实现它
#[async_trait]
pub trait AlienProvider: Send + Sync + Debug {
    async fn request(&self, target: AlienTarget) -> Result<Arc<AlienResponse>, AlienError>;
    fn get_endpoint(&self, chain: Chain) -> Result<String, AlienError>;
}
```

这是整个项目**最重要的架构决策**：Rust 不做网络 IO，由平台注入。好处：

- Rust 侧不打包 reqwest（`reqwest_provider` 只是测试用的 feature），也就避开了把 `ring`/`openssl` 交叉编译到 NDK 的麻烦
- 复用平台的 URLSession / OkHttp —— 连接池、代理、证书固定、拦截器、抓包全都继承
- `async fn` 直接映射到 Swift `async/await` 和 Kotlin `suspend`

> 完整链路和两端实现见 [第 8 章](#8-深入网络请求为什么委托给平台怎么做的)。

### 错误类型

```rust
#[derive(Debug, uniffi::Error)]
pub enum GemstoneError {
    AnyError    { msg: String },
    SignerError { error: signer::GemSignerError, msg: String },
}
```

到 Swift 是 `enum GemstoneError: Error`，到 Kotlin 是 `GemstoneException`（注意 Kotlin 侧后缀是 `Exception` 不是 `Error`）。

---

## 4. iOS 构建链路详解

入口：`just ios generate-stone` → `ios/scripts/generate-stone.sh`

这个脚本做两件事。

### ① 生成 Swift 绑定 `generate_bindings()`

先算一个**源码指纹**决定要不要重跑：

```bash
shasum -a 256 "$0"                      # 脚本自身
git -C core rev-parse HEAD              # core 的 commit
git -C core diff                        # 未提交改动
git -C core diff --cached               # 已暂存改动
git -C core ls-files --others ... stat  # untracked 文件的 mtime/size
```

哈希存在 `ios/build/.gemstone-cache/sources-{debug|release}.hash`，没变就跳过。`--force` 强制重跑（`just ios generate-stone` 默认带 `--force`）。

然后跑 `just bindgen-swift`，把产物拷进 SPM 包：

```
core/gemstone/generated/swift/gemstone.swift  →  ios/Packages/Gemstone/Sources/Gemstone/Gemstone.swift
core/gemstone/generated/swift/GemstoneFFI.h   →  ios/Packages/Gemstone/Sources/GemstoneFFI/include/GemstoneFFI.h
```

> 这两个目录在 `ios/.gitignore:42`，**不入库**。刚 clone 下来是空的，所以第一次必须先跑生成。

### ② 编译静态库 `build_ios_static_libraries()`

```bash
cargo rustc --manifest-path core/gemstone/Cargo.toml \
    --target aarch64-apple-ios-sim \    # 模拟器
    --target aarch64-apple-ios \        # 真机
    --lib --crate-type staticlib
```

几个讲究的地方：

- `IPHONEOS_DEPLOYMENT_TARGET` 用 `PlistBuddy` **从 `Gem.xcodeproj/project.pbxproj` 里读出来**，保证 Rust 和 Xcode 用同一个部署目标，不然链接会警告甚至报错
- 主动 `unset MACOSX_DEPLOYMENT_TARGET` 等一堆变量 —— Xcode 调用时会注入这些，会污染 cargo 构建
- Release 加 `CARGO_PROFILE_RELEASE_LTO=fat` + `CODEGEN_UNITS=1`，体积和性能都更好
- 显式拒绝 Intel Mac（`uname -m != arm64` 直接报错退出）

### ③ 链接到 App

SPM 包只声明 Swift 层（`ios/Packages/Gemstone/Package.swift`）：

```swift
targets: [
    .target(name: "Gemstone", dependencies: ["GemstoneFFI"]),
    .target(name: "GemstoneFFI", path: "Sources/GemstoneFFI", publicHeadersPath: "include")
]
```

真正的 `.a` 由 Xcode 工程按 SDK 分别搜索（`Gem.xcodeproj/project.pbxproj`）：

```
LIBRARY_SEARCH_PATHS[sdk=iphoneos*]        = $(SRCROOT)/../core/target/aarch64-apple-ios/{debug,release}
LIBRARY_SEARCH_PATHS[sdk=iphonesimulator*] = $(SRCROOT)/../core/target/aarch64-apple-ios-sim/{debug,release}
OTHER_LDFLAGS                              = -lgemstone
```

`ios/justfile` 里跑测试时还会额外拼一段 `GEMSTONE_LINKER_FLAGS` 把模拟器目录塞进去。

### ⚠️ iOS 最大的坑

`grep PBXShellScriptBuildPhase Gem.xcodeproj/project.pbxproj` 结果是 **0** —— **Xcode 里没有任何 Run Script 阶段去调 Rust**。

后果：改了 Rust 忘了跑 `just generate-stone`，Xcode 会**静默用旧的 `libgemstone.a` 链接成功**，你以为改生效了其实没有。而且因为绑定文件也没变，编译期一点提示都没有。

**改 Rust 后跑 `just ios generate-stone`，养成条件反射。**

（相比之下 Android 是全自动的，见下一章。这个不对称是最容易让人栽跟头的地方。）

---

## 5. Android 构建链路详解

### 壳模块设计

`android/gemstone/` 目录里**只有一个 `build.gradle.kts`**，没有任何源码。它把源码目录指向 core：

```kotlin
val gemstoneRoot = rootProject.projectDir.resolve("../core/gemstone")
val gemstoneSrc  = gemstoneRoot.resolve("android/gemstone/src")

sourceSets.getByName("main") {
    kotlin  { directories.add(gemstoneSrc.resolve("main/java").absolutePath) }      // uniffi 生成的 .kt
    jniLibs { directories.add(gemstoneSrc.resolve("main/jniLibs").absolutePath) }   // .so
    manifest.srcFile(gemstoneSrc.resolve("main/AndroidManifest.xml"))
}
```

### 两个 Exec task

```kotlin
val bindgenKotlin = tasks.register<Exec>("bindgenKotlin") {
    inputs.dir(rustSrcDir); inputs.dir(cratesDir); inputs.file("Cargo.toml")
    outputs.dir(generatedKotlinDir.resolve("uniffi"))
    commandLine("/bin/sh", "-l", "-c", "just bindgen-kotlin")
}

val buildCargoNdk = tasks.register<Exec>("buildCargoNdk") {
    inputs.dir(rustSrcDir); inputs.dir(cratesDir); inputs.file("Cargo.toml")
    outputs.dir(jniLibsDir)
    commandLine("/bin/sh", "-l", "-c",
        "cargo ndk $cargoNdkTargets -o ${jniLibsDir} build --lib ${cargoBuildFlag}")
}
```

声明了 `inputs` / `outputs`，所以 Gradle 会做**增量判断** —— Rust 没改就不重跑。

### 自动挂钩

```kotlin
tasks.configureEach {
    if (name.matches(Regex("(compile|extract|source|javaDoc).*(Debug|Release).*")))
        dependsOn(bindgenKotlin)
    if (name.matches(Regex("merge.*(Debug|Release).*JniLib.*")))
        dependsOn(buildCargoNdk)
}
```

**所以 `./gradlew assembleGoogleDebug` 会自动编 Rust，不需要任何额外命令。**

### ABI 策略

```kotlin
val defaultCargoNdkAbis = if (System.getenv("UNIT_TESTS") == "true") "x86_64"
                          else "arm64-v8a,armeabi-v7a"
val cargoNdkTargets = (System.getenv("GEMSTONE_ANDROID_ABIS") ?: defaultCargoNdkAbis)...
```

| 场景 | ABI |
|---|---|
| 默认（真机） | `arm64-v8a`, `armeabi-v7a` |
| `UNIT_TESTS=true` | `x86_64` |
| 自定义 | `GEMSTONE_ANDROID_ABIS=arm64-v8a,x86_64` |

### 运行时加载

用 **JNA**，不是手写 JNI：

```kotlin
dependencies { api("net.java.dev.jna:jna:5.18.1@aar") }
```

UniFFI 生成的 Kotlin 通过 JNA 的 `Native.load("gemstone", ...)` 动态加载 `.so`，函数签名全自动映射。

### JVM 单元测试怎么办

模拟器/真机跑的是 `.so`，但 JVM 单测跑在开发机上。所以：

```
just android test
  → build-gemstone-host      # cd core && cargo build --package gemstone
  → ./gradlew testGoogleDebugUnitTest
```

JNA 会从 `core/target/debug/libgemstone.dylib`（macOS）加载宿主机版本。

### 对外出口

```kotlin
// android/gemcore/build.gradle.kts:58
api(project(":gemstone"))
```

所有 feature 模块依赖 `:gemcore` 就能拿到 Rust 能力。

### 另一套独立工程

`core/gemstone/android/` 下还有一个**完整独立的 Gradle 工程**（`rootProject.name = "gemstone-android"`），配了 `maven-publish`：

```bash
just gemstone build-android   # → publishDebugPublicationToMavenLocal
```

主 App **不走这条路**，它是为发 AAR 准备的。第 9 章会用到。

---

## 6. 开发流程：从环境搭建到提交

### 6.1 首次环境搭建（各做一次）

```bash
git clone <repo> && cd wallet
just setup-git                    # 初始化 git submodule

# iOS
cd ios && just bootstrap
# = rust + typeshare-cli + iOS rust targets + swiftgen/swiftformat + 首次 generate-stone

# Android
cd android && just install-java   # Temurin JDK 17（如果没有）
cd android && just bootstrap
# = rust + android rust targets + cargo-ndk@4.1.2 + NDK 28.1.13356709
```

装的 target 清单（`core/gemstone/justfile`）：

```bash
# iOS
rustup target add aarch64-apple-ios-sim aarch64-apple-ios \
                  aarch64-apple-ios-macabi aarch64-apple-darwin
# Android
rustup target add x86_64-linux-android aarch64-linux-android armv7-linux-androideabi
cargo install cargo-ndk@4.1.2
```

### 6.2 场景 A：只改 App 层代码（不碰 Rust）

```bash
cd ios     && just build && just run
cd android && just build && just run
```

`just run` 会自动 boot 模拟器 / 找设备、安装、启动、串 logcat。Android 的 `_run-adb` 还会等进程起来再抓 PID 过滤日志，起不来就 dump 最近 500 行错误。

常用变体：

```bash
SIMULATOR_NAME="iPhone 16 Pro" just ios run    # 换模拟器
cd android && just run-device                  # 装真机而不是模拟器
```

### 6.3 场景 B：改了 Rust

严格按 `skills/new-feature-workflow.md` 的顺序，**永远从 Core 往外做**，不要先在 App 层写桩：

```bash
# ── 第 1 步：Core 自测 ──
cd core
just test <CRATE>
cargo clippy -p <crate> -- -D warnings
just format

# ── 第 2 步：重新生成绑定 ──
cd .. && just generate            # 两端全量：models + stone
# 或者按需：
cd ios && just generate-stone     # UniFFI 绑定 + .a
cd ios && just generate-models    # typeshare 模型
```

**什么时候需要重新生成？**

| Core 的改动 | 要重新生成吗 |
|---|---|
| 改了 `#[uniffi::export]` 的签名 | ✅ 必须 |
| 给 `primitives` 加了字段/类型 | ✅ 必须 |
| 只改了函数内部实现，签名不变 | ❌ 不需要（但 iOS 需要重编 `.a`） |
| 只加了 Rust 内部的私有 helper | ❌ 不需要 |

> 注意区分：**签名没变但实现变了，iOS 仍然要重编 `.a`**（`just generate-stone` 会跳过绑定生成但仍然编库）。Android 会自动处理。

```bash
# ── 第 3 步：两端消费 ──
cd ios     && just build-package <PACKAGE>        # 快速迭代单个包
cd ios     && just test <TARGET>
cd ios     && just build                          # 完整

cd android && ./gradlew :<module>:assembleDebug   # 快速迭代单个模块
cd android && ./gradlew :<module>:testDebugUnitTest
cd android && just build                          # 完整
```

### 6.4 场景 C：改文案

```bash
# 编辑 localization/app/en.ftl，以及所有其他语言文件
just localize     # 生成 .strings / strings.xml，iOS 还会跑 swiftgen
```

规则（见根 `CLAUDE.md`）：

- 消息 ID 用下划线，如 `common_cancel`
- 按前缀放到对应 section（`common_*` 归 `# Common`）
- **新 key 必须加到每一个语言文件**并翻译，不要靠 fallback 掩盖
- iOS Info.plist 文案在 `localization/InfoPlist/`，widget 在 `localization/widget/`
- 生成产物**不要手改**

### 6.5 提交前

```bash
cd core    && just lint && just format     # clippy -D warnings + cargo fmt
cd ios     && just lint && just format     # swiftlint --fix + swiftformat
cd android && just lint
```

> 项目约定：**开发过程中用targeted命令，最后一次性跑批量验证**。不要每改一行就跑全量构建。

### 6.6 CI 是怎么跑的

`.github/workflows/ios-ci.yml`：

```yaml
- name: Build Core
  working-directory: ios
  run: just generate-stone
  env:
    GEMSTONE_IOS_TARGETS: "aarch64-apple-ios-sim"   # 只编模拟器架构提速
- run: just spm-resolve
- run: just build
```

Release job 用 `BUILD_MODE=release just generate-stone`。

`.github/workflows/android-ci.yml`：

```yaml
- run: just install-toolchains && just install-ndk
- run: just build      # Rust 由 Gradle 自动带起来
- run: just test
```

注意两个 workflow 的 `paths` 都包含 `core/**` —— Core 改动会触发两端 CI。

---

## 7. 应用层怎么调用 Rust

### ① TypeShare 模型 —— 纯数据，零开销

```swift
import Primitives
let chain: Chain = .ethereum
```

```kotlin
import com.wallet.core.primitives.FiatQuoteType
```

就是普通的 Swift struct / Kotlin data class，没有任何 FFI 开销。

**规则：不要手改生成文件。** 要加行为就写扩展：

```swift
// 单独文件 ChainExtensions.swift
extension Chain {
    var displayName: String { ... }
}
```

### ② UniFFI 顶层函数

Rust：

```rust
#[uniffi::export]
pub fn deeplink_build_url(deeplink: Deeplink) -> String { ... }
```

Swift：

```swift
import Gemstone
let url = deeplinkBuildUrl(deeplink: link)
```

Kotlin：

```kotlin
import uniffi.gemstone.Deeplink
import uniffi.gemstone.deeplinkBuildUrl

internal fun Deeplink.toRoute(): NavKey? = when (this) {
    is Deeplink.Asset  -> assetId.toAssetId()?.let(::AssetRoute)
    is Deeplink.Buy    -> assetId.toAssetId()?.let { FiatInputRoute(it, amount, FiatQuoteType.Buy) }
    Deeplink.Perpetuals -> null
}
```

> 实例见 `android/app/src/main/kotlin/com/gemwallet/android/WebDeepLinks.kt`。

### ③ UniFFI 对象 —— 有状态实例

```swift
import class Gemstone.Config
Gemstone.Config.shared.getBlockExplorers(chain: chain.id)
// ios/Packages/ChainServices/ExplorerService/ExplorerService.swift:20
```

```kotlin
import uniffi.gemstone.Config
Config().getBlockExplorers(chain)
```

这类是 `#[derive(uniffi::Object)]`，两端持有的是 Rust 对象句柄，析构时自动 free（Swift 靠 deinit，Kotlin 靠 `AutoCloseable` / Cleaner）。

iOS 侧还有个 `GemstonePrimitives` 包做了封装：

```swift
import enum GemstonePrimitives.GemstoneConfig
GemstoneConfig.shared.getValidators()
GemstoneConfig.shared.swapConfig().highSlippageWarningBps
```

### ④ 反向回调 —— 平台给 Rust 提供能力 ⭐

前三种是「平台调 Rust」，这一种是**反过来：Rust 定义 trait，平台实现，注入进去，然后 Rust 调平台**。

全仓库只有**两个**这样的 trait：

```bash
$ grep -rn "with_foreign" core/gemstone/src
alien/provider.rs:8       # AlienProvider   —— 网络请求
gateway/preferences.rs:4  # GemPreferences  —— 键值存储
```

```rust
#[uniffi::export(with_foreign)]     // ★ with_foreign = 允许外部语言实现
#[async_trait]
pub trait AlienProvider: Send + Sync + Debug {
    async fn request(&self, target: AlienTarget) -> Result<Arc<AlienResponse>, AlienError>;
    fn get_endpoint(&self, chain: Chain) -> Result<String, AlienError>;
}
```

平台侧实现后在构造 `GemGateway` 时传进去：

```swift
gateway = GemGateway(provider: provider, preferences: ..., securePreferences: ..., apiUrl: ...)
```
```kotlin
GemGateway(alienProvider, preferences = ..., securePreferences = ..., apiUrl = ...)
```

> 这是全项目**最重要的架构决策**，值得单独一章 —— 见 [第 8 章](#8-深入网络请求为什么委托给平台怎么做的)。

**跨语言类型对照**（容易踩）：

| Rust | Swift | Kotlin |
|---|---|---|
| `u16` | `UInt16` | `UShort`（要写 `200.toUShort()`） |
| `Vec<u8>` | `Data` | `ByteArray` |
| `Option<T>` | `T?` | `T?` |
| `Result<T, E>` | `throws -> T` | `@Throws` + Exception |
| `async fn` | `async throws` | `suspend fun` |
| `enum MyError` | `enum MyError: Error` | `class MyException` ← **后缀不同** |

---

## 8. 深入：网络请求为什么委托给平台，怎么做的

### 8.1 一句话

**Rust 里所有需要发 HTTP 的地方，都不自己发，而是调一个由 Swift / Kotlin 实现的接口。**

常规做法是 Rust 里 `cargo add reqwest`，自己带一整套 HTTP + TLS 栈。这个项目反过来：Rust 只负责「我要请求这个 URL、这个方法、这个 body」，真正的 socket 由 `URLSession` / `OkHttp` 去发。

### 8.2 为什么

| 理由 | 说明 |
|---|---|
| **躲开交叉编译地狱** | reqwest 默认依赖 `ring` / `openssl` 这类含 C 和汇编的 crate。编到 `aarch64-linux-android` / `armv7-linux-androideabi` 要配 NDK sysroot，是移动端 Rust 最常见的构建噩梦。委托出去之后 Rust 侧**纯 Rust 无 C 依赖** |
| **二进制体积** | 不打包 TLS 栈和 HTTP/2 实现，`.so` / `.a` 小一大截 |
| **继承平台网络能力** | 连接池复用、HTTP/2、系统代理、企业 VPN、蜂窝/WiFi 策略、后台传输、Charles/Proxyman 抓包 —— 全都白拿。自带 reqwest 的话这些全部失效，尤其**抓不到包**，调试极其痛苦 |
| **平台可插拦截器** | Android 给 OkHttp 挂 `nodeAuthInterceptor` 做请求签名；iOS 用 `RequestInterceptable`。Rust 完全不需要知道鉴权怎么做 |
| **节点 URL 归平台管** | `get_endpoint(chain)` 也在这个接口里。用户在设置里换 RPC 节点，Rust 侧零改动 |
| **可测试** | `TestAlienProvider` 直接返回固定响应，单测不起网络 |

### 8.3 完整链路：一次余额查询走过的路

```
Swift: gateway.getBalanceCoin(chain:address:)
  │
  ├─ FFI ──────────────────────────────────────────────────┐
  ▼                                                        │
Rust: GemGateway → ChainFactory → dyn ChainTraits          │
  │   （某条链的客户端，比如 Solana）                          │
  ▼                                                        │
gem_client::Client            ← 业务代码看到的抽象            │
  │  .get_with("/balance", query, headers)                 │
  ▼                                                        │
gem_jsonrpc::RpcClient<E>     ← 拼 URL / 序列化 body /       │
  │                              处理 Content-Type          │
  │  组装出 Target { url, method, headers, body }           │
  ▼                                                        │
gem_jsonrpc::RpcProvider      ← 只管「发 Target 拿 Response」 │
  ▲ impl                                                   │
AlienProviderWrapper          ← 适配器                       │
  │  包着 Arc<dyn AlienProvider>                            │
  ▼                                                        │
★ dyn AlienProvider  ═══════ FFI 边界 ═══════════════════════┘
  │
  ▼
Swift: NativeProvider.request(target)
  │  target.asRequest() → URLRequest
  │  requestInterceptor.intercept(&request)
  ▼
URLSession.data(for: request)   ← 真正发包
  │
  ▼
AlienResponse(status:data:) ══ FFI 回到 Rust ══► deserialize_response::<R>()
```

关键点：**FFI 边界被压到了整个网络栈的最底层。** 上面所有的路由、序列化、错误处理、重试逻辑都在 Rust 里，跨语言只传「原始请求 / 原始字节」，接口面极小 —— 只有两个方法。

### 8.4 Rust 侧逐层拆解

**① 接口定义** —— `core/gemstone/src/alien/provider.rs:8`

```rust
#[uniffi::export(with_foreign)]
#[async_trait]
pub trait AlienProvider: Send + Sync + Debug {
    async fn request(&self, target: AlienTarget) -> Result<Arc<AlienResponse>, AlienError>;
    fn get_endpoint(&self, chain: Chain) -> Result<String, AlienError>;
}
```

普通的 `#[uniffi::export]` 是「Rust 实现、平台调用」；加上 `with_foreign` 变成双向 —— 平台实现它，Rust 持有 `Arc<dyn AlienProvider>` 反过来调。UniFFI 会在两端生成回调注册表和 vtable。

**② 传输结构** —— `core/gemstone/src/alien/target.rs`

```rust
pub type AlienTarget = swapper::Target;

#[uniffi::remote(Record)]        // remote = 类型定义在别的 crate，这里只声明 FFI 形状
pub struct AlienTarget {
    pub url: String,
    pub method: AlienHttpMethod,     // Get/Post/Put/Delete/Head/Options/Patch
    pub headers: Option<HashMap<String, String>>,
    pub body: Option<Vec<u8>>,
}

#[derive(Debug, uniffi::Object)]
pub struct AlienResponse {
    response: RpcResponse,           // { status: Option<u16>, data: Vec<u8> }
}
```

就是一个裸的 HTTP 请求/响应描述，没有任何业务语义。

**③ 适配器** —— `core/gemstone/src/alien/provider.rs:16`

```rust
#[async_trait]
impl GenericRpcProvider for AlienProviderWrapper {
    type Error = AlienError;
    async fn request(&self, target: AlienTarget) -> Result<RpcResponse, Self::Error> {
        Ok(self.provider.request(target).await?.to_rpc_response())
    }
    fn get_endpoint(&self, chain: Chain) -> Result<String, Self::Error> {
        self.provider.get_endpoint(chain)
    }
}
```

把 UniFFI 的 `AlienProvider`（返回 `Arc<AlienResponse>`，FFI 对象需要引用计数）适配成 Rust 内部通用的 `RpcProvider`（返回裸 `RpcResponse`）。**这样 `core/crates/**` 里的链实现完全不知道 UniFFI 的存在** —— 同一套链逻辑既能给移动端用，也能给 `apps/api` 这类服务端用（那边注入的是真 reqwest）。

**④ 上层抽象** —— `core/crates/gem_jsonrpc/src/rpc.rs:80`

```rust
impl<E: RpcClientError> Client for RpcClient<E> {
    async fn get_with<R: DeserializeOwned>(&self, path: &str, _query: &[..], headers: HashMap<..>) -> Result<R, ClientError> {
        let target = Target {
            url: build_request_url(&self.base_url, path),
            method: HttpMethod::Get,
            headers: if headers.is_empty() { None } else { Some(headers) },
            body: None,
        };
        let response = self.provider.request(target).await.map_err(|e| e.into_client_error())?;
        deserialize_response(&response)       // ← 泛型反序列化回业务类型
    }
    // post_with 还会按 Content-Type 决定 body 编成 JSON / hex / form / bcs
}
```

业务代码只看到 `gem_client::Client` 这个 trait，写 `client.get::<Balance>("/v1/balance").await`，完全感知不到底下是平台在发包。

**⑤ 注入点** —— `core/gemstone/src/gateway/mod.rs:53`

```rust
#[uniffi::export]
impl GemGateway {
    #[uniffi::constructor]
    pub fn new(
        provider: Arc<dyn AlienProvider>,       // ← 平台传进来
        preferences: Arc<dyn GemPreferences>,
        secure_preferences: Arc<dyn GemPreferences>,
        api_url: String,
    ) -> Self { ... }
}
```

### 8.5 平台侧实现

#### iOS —— `ios/Packages/NativeProviderService/`

```swift
public actor NativeProvider {            // actor：天然线程安全
    private let session: URLSession
    private let nodeProvider: any NodeURLFetchable
    private let cache: MemoryCache
    private let requestInterceptor: any RequestInterceptable
}

extension NativeProvider: AlienProvider {
    public func request(target: AlienTarget) async throws -> AlienResponse {
        if let key = target.nativeCacheKey, let data = await cache.get(key: key) {
            return AlienResponse(status: 200, data: data)        // 缓存命中，不发包
        }
        var request = try target.asRequest()
        requestInterceptor.intercept(request: &request)          // 插鉴权头等
        let (data, response) = try await session.data(for: request)
        let statusCode = (response as? HTTPURLResponse)?.statusCode
        if let key = target.nativeCacheKey { await cache.set(key: key, value: data) }
        return AlienResponse(status: statusCode.map(UInt16.init), data: data)
    }

    public nonisolated func getEndpoint(chain: Chain) throws -> String { ... }
}
```

`AlienTarget → URLRequest` 的转换在 `URLRequestSequence.swift:42`：

```swift
extension AlienTarget: URLRequestConvertible {
    func asRequest() throws -> URLRequest {
        guard let url = URL(string: url) else { throw AlienError.RequestError(msg: "invalid url: \(url)") }
        var request = URLRequest(url: url)
        request.httpMethod = alienMethodToString(method: method)
        request.allHTTPHeaderFields = headers?.filter { $0.key != nativeProviderCacheHeader }
        request.httpBody = body
        return request
    }
}
```

装配在 `ios/Packages/Blockchain/Sources/Gateway/GatewayService.swift:17`：

```swift
gateway = GemGateway(
    provider: provider,
    preferences: GemstonePreferences(namespace: "gateway"),
    securePreferences: GemstoneSecurePreferences(namespace: "gateway"),
    apiUrl: Constants.apiURL.absoluteString,
)
```

#### Android —— `android/data/services/remote-gem/`

```kotlin
class NativeProvider(
    private val getNodeUrlCase: GetNodeUrlCase,
    private val httpClient: OkHttpClient = OkHttpClient(),
    private val config: NativeProviderConfig,
) : AlienProvider {

    override fun getEndpoint(chain: Chain): String =
        chain.toChain()?.let(getNodeUrlCase::getNodeUrl)
            ?: throw GatewayException.PlatformException("Can't found node url for chain: $chain")

    override suspend fun request(target: AlienTarget): AlienResponse = withContext(Dispatchers.IO) {
        cache.get(target.nativeCacheKey())?.let { return@withContext AlienResponse(200.toUShort(), it) }

        val builder = Request.Builder().url(target.url)
            .method(target.method.name, target.body?.toRequestBody())
        target.headers?.forEach { (k, v) -> if (k != NATIVE_PROVIDER_CACHE_HEADER) builder.addHeader(k, v) }

        try {
            httpClient.newCall(builder.build()).execute().use {
                AlienResponse(it.code.toUShort(), it.body.bytes())
            }
        } catch (err: IOException) {
            throw AlienException.RequestException(err.toGatewayNetworkMessage(config.networkOfflineMessage))
        }
    }
}
```

Hilt 装配（`android/app/src/main/kotlin/com/gemwallet/android/di/GatewayModule.kt:50`）—— 注意这里把鉴权拦截器挂进 OkHttp：

```kotlin
@Provides @Singleton
fun provideAlienProvider(...): AlienProvider = NativeProvider(
    getNodeUrlCase = getNodeUrlCase,
    httpClient = okHttpClient.newBuilder().addInterceptor(nodeAuthInterceptor).build(),
    config = NativeProviderConfig(networkOfflineMessage = context.getString(UiR.string.errors_network_offline)),
)

@Provides @Singleton
fun provideGateway(alienProvider: AlienProvider, ...) = GemGateway(
    alienProvider,
    preferences = SharedGemPreferences(context.getSharedPreferences("gateway_preferences", MODE_PRIVATE)),
    securePreferences = securePreferences,
    apiUrl = Constants.API_URL,
)
```

### 8.6 三个值得学的设计细节

#### ① 用 HTTP header 当「跨 FFI 控制通道」传缓存策略

Rust 侧想让某个请求被缓存，不需要给 `AlienProvider` 加参数 —— 往 header 里塞一个私有键：

```rust
// core/crates/gem_client/src/lib.rs:38
pub const X_CACHE_TTL: &str = "x-gem-cache-ttl";

// core/crates/swapper/src/cache.rs
HashMap::from([(X_CACHE_TTL.to_string(), MONTH.as_secs().to_string())])
```

平台看到这个 header 才生成缓存 key（`ios/Packages/NativeProviderService/Sources/NativeProviderCache.swift`）：

```swift
var nativeCacheKey: String? {
    guard headers?[nativeProviderCacheHeader] != nil else { return nil }   // 没这个 header 就不缓存
    var hasher = SHA256()
    hasher.update(data: Data(alienMethodToString(method: method).utf8))
    hasher.update(data: cacheKeySeparator)        // \0 分隔，防止拼接歧义
    hasher.update(data: Data(url.utf8))
    if let body { hasher.update(data: cacheKeySeparator); hasher.update(data: body) }
    return Data(hasher.finalize()).base64EncodedString()
}
```

然后**在真正发包前把这个 header 摘掉**（两端的 `asRequest()` / builder 里都有 filter），不泄漏给服务器。

好处：FFI 接口签名保持极简，加新的控制维度（超时、重试、优先级）不需要改契约，也就不需要下游同步升级。

#### ② 请求合并（coalescing）—— 纯 Rust 侧的装饰器

`core/gemstone/src/alien/coalescing_provider.rs`：同一个 `(method, url, body)` 并发发起多次，只真正打一次网络，其余的挂在 `oneshot::channel` 上等结果。

```rust
pub(crate) fn coalescing_provider(provider: Arc<dyn AlienProvider>) -> Arc<dyn AlienProvider> {
    Arc::new(CoalescingAlienProvider::new(provider))
}
```

用在 `WalletConnectSimulationClient::new` —— 交易模拟会被多个 UI 组件同时触发。

细节：有个 `RequestGuard` 的 `Drop` 实现，请求被取消时会把所有等待者用 `Err("Coalesced request was canceled")` 唤醒，避免永久挂起。这类装饰器忘了处理取消路径就会造成 UI 卡死。

注意它是**装饰器模式套在 `dyn AlienProvider` 外面**，平台完全无感 —— 这就是把 FFI 边界定义成 trait（而不是一堆自由函数）的好处。

#### ③ 测试时用真 reqwest 顶替平台

```rust
// core/gemstone/src/alien/reqwest_provider.rs
#[cfg(feature = "reqwest_provider")]
impl AlienProvider for NativeProvider { ... }    // swapper::NativeProvider，基于 reqwest
```

集成测试（`just test-integration` 里的 `--features swap_integration_tests`）用它，这样 `cargo test` 不需要模拟器也能打真网络。生产构建不含这个 feature，reqwest 不进二进制。

### 8.7 同样的模式还用在存储

另一个 `with_foreign` trait（`core/gemstone/src/gateway/preferences.rs:4`）：

```rust
#[uniffi::export(with_foreign)]
pub trait GemPreferences: Send + Sync {
    fn get(&self, key: String) -> Result<Option<String>, GatewayError>;
    fn set(&self, key: String, value: String) -> Result<(), GatewayError>;
    fn remove(&self, key: String) -> Result<(), GatewayError>;
}
```

iOS 用 `UserDefaults` / Keychain 实现，Android 用 `SharedPreferences` / EncryptedSharedPreferences 实现。Rust 侧再包一层 `PreferencesWrapper` 转成内部的 `primitives::Preferences`，同样对 `crates/**` 屏蔽 UniFFI。

> **统一原则：凡是「平台已经做得比 Rust 好」的能力（网络栈、安全存储、生物识别），一律定义 trait 委托出去；Rust 只保留纯计算和业务逻辑。**

### 8.8 代价

这套设计不是白给的，四个要注意的地方：

1. **每次 HTTP 都跨一次 FFI**，且 `async` 跨语言需要 UniFFI 的 future 桥接。钱包这种请求量不大的场景无所谓，高频轮询要留意
2. **两端各实现一遍，行为必须一致** —— 缓存 key 算法、header 过滤、错误映射，iOS 和 Android 写两份，容易漂移。靠 `NativeProviderCacheTests` 之类的测试兜底
3. **错误类型要手工对齐**：`AlienError::{RequestError, ResponseError, Http}` 在 Swift 是 `AlienError`，在 Kotlin 是 `AlienException`，两端 catch 写法不同
4. **超时/重试的归属容易含糊**：现在超时在平台侧（OkHttp 的 `callTimeout`、URLSession 配置），重试策略在 Rust（`gem_client::retry`）。这条线要划清楚，不然会双重重试

---

## 9. 进阶：Core 独立成仓后如何分发

如果你只维护 `core`，iOS / Android 在别的仓库 —— **不要直接把生成的文件发给他们**。

### 为什么不能直接给文件

UniFFI 生成的绑定里带了 `uniffiCheckContractApiVersion()` 和 `uniffiCheckApiChecksums()`。绑定文件和原生库版本对不上，**App 启动时直接 crash，不是编译期报错**。手工拷贝文件迟早踩这个，而且排查成本极高。

**正确做法：绑定 + 原生库必须同一次构建产出，打成版本化制品发布。**

### 9.1 Android —— 几乎现成

`core/gemstone/android/gemstone/build.gradle.kts` 已经配好 `maven-publish`：

```kotlin
create<MavenPublication>("release") {
    from(components["release"])
    groupId    = "com.gemwallet.gemstone"
    artifactId = "gemstone"
    version    = System.getenv("VER_NAME") ?: "1.0.0"
}
```

现在只推 mavenLocal，改成推远端即可：

```bash
./gradlew publishReleasePublicationToGitHubPackagesRepository
```

产出的 **AAR 是自包含的**：`jniLibs/{abi}/libgemstone.so` + 生成的 `uniffi/*.kt` + JNA 传递依赖。

下游 Android 仓库一行搞定，**完全不需要装 Rust / NDK / cargo-ndk**：

```kotlin
implementation("com.gemwallet.gemstone:gemstone:2.114.10")
```

> 发布版 ABI 要打全：`GEMSTONE_ANDROID_ABIS=arm64-v8a,armeabi-v7a,x86_64`。x86_64 是模拟器必需的，漏了下游开发在模拟器上跑不起来。

### 9.2 iOS —— 需要补三步

当前的裸 `.a` + `LIBRARY_SEARCH_PATHS` 是 **monorepo 专属**的，跨仓库路径不存在，必须改。

**第 1 步：开启 modulemap**

```toml
# core/gemstone/uniffi.toml
[bindings.swift]
generate_module_map = true      # 原来是 false
```

`binaryTarget` 要求 xcframework 里有 headers + modulemap，monorepo 里靠 SPM 的 `publicHeadersPath` 绕过去了，跨仓库不行。

**第 2 步：打 XCFramework**

在 `build_ios_static_libraries()` 之后加：

```bash
xcodebuild -create-xcframework \
  -library core/target/aarch64-apple-ios/release/libgemstone.a     -headers include/ \
  -library core/target/aarch64-apple-ios-sim/release/libgemstone.a -headers include/ \
  -output GemstoneFFI.xcframework

zip -r GemstoneFFI.xcframework.zip GemstoneFFI.xcframework
swift package compute-checksum GemstoneFFI.xcframework.zip
```

> 真机 + 模拟器**必须都打**，否则下游只能在其中一种上跑。需要 Mac Catalyst 就加 `aarch64-apple-ios-macabi`（target 已经装了）。

**第 3 步：建独立分发仓库 `gemstone-swift`**

```swift
// swift-tools-version: 6.0
let package = Package(
    name: "Gemstone",
    platforms: [.iOS(.v17), .macOS(.v15)],
    products: [.library(name: "Gemstone", targets: ["Gemstone"])],
    targets: [
        // Gemstone.swift 作为源码入库
        .target(name: "Gemstone", dependencies: ["GemstoneFFI"]),
        // .a 从 Release 下载
        .binaryTarget(
            name: "GemstoneFFI",
            url: "https://github.com/<org>/gemstone-swift/releases/download/2.114.10/GemstoneFFI.xcframework.zip",
            checksum: "abc123..."
        )
    ]
)
```

下游 iOS 仓库：

```swift
.package(url: "https://github.com/<org>/gemstone-swift", from: "2.114.10")
```

> **私有分发注意**：`binaryTarget` 的 URL 不支持带鉴权头。内网场景要么放 S3 预签名 / 内网 HTTP，要么改用 CocoaPods（podspec + `vendored_frameworks` + 私有 spec repo），CocoaPods 对私有分发更友好。

### 9.3 TypeShare 模型单独走

纯源码，零原生依赖，和 gemstone 是**两套独立类型**（Kotlin 侧一个 `com.wallet.core.primitives.*`，一个 `uniffi.gemstone.*`）。

- iOS → 一个 source-only SPM package
- Android → 纯 Kotlin/JVM jar，或干脆塞进同一个 AAR 省事

技术上可以独立版本，但**建议统一用 core workspace 的版本号**（`core/Cargo.toml` 已经是 `version = "2.114.10"`），让下游只对齐一个数字，别搞版本矩阵。

### 9.4 发布流水线

```
core 打 tag v2.114.10
        │
        ├─ CI(macOS) ─► generate-stone(release, 双架构)
        │               ├─ GemstoneFFI.xcframework.zip ──► GitHub Release
        │               └─ Gemstone.swift ──► push 到 gemstone-swift 仓库 + 打 tag
        │
        └─ CI ─► gradlew publish ──► Maven registry
                 (AAR 内含 .so + .kt + JNA 依赖)
```

现有的 `.github/workflows/release_on_tag.yml` 只建了个空 Release，把上面两步加进去就完整了。

### 9.5 不推荐的三种做法

| 做法 | 为什么不行 |
|---|---|
| ❌ 直接发 `.swift` / `.kt` 让下游提交 | 原生库怎么办？下游没有 Rust 工具链。checksum 错位 = 运行时 crash |
| ❌ 下游把 core 当 submodule 本地编 | 每个客户端开发都要装 Rust + NDK + cargo-ndk，CI 从几分钟变几十分钟。只有团队 <5 人且全栈时才划算 |
| ⚠️ submodule + 预编译产物缓存 | 能省 CI 时间，但缓存失效逻辑很难做对，复杂度不比发包低 |

### 9.6 你会新增的两项职责

**① 你成了上游 API owner。**

`#[uniffi::export]` 的任何签名变更都是 breaking change，下游直接编译不过。建议：

- 严格语义化版本，接口删改走 major
- 维护 CHANGELOG，每次发版明确列出「新增 / 变更 / 移除的 UniFFI 接口」
- 破坏性变更先加新接口 + 标 `#[deprecated]`，给下游一个版本的过渡窗口

**② 提供下游冒烟工程。**

这个你已经有了 —— `core/gemstone/tests/ios/GemTest` 和 `core/gemstone/tests/android/GemTest` 是两个**独立的消费方示例工程**（`just gemstone build-ios` / `test-ios` 用的就是它们）。

把它们改造成「从远端 registry 拉包」而不是本地路径引用，就变成了**发布验证关**：CI 里发完包再用它们拉一次、装一次，能跑起来才算发布成功。这一步能挡掉绝大多数版本错位事故。

---

## 10. 常见坑速查

| 症状 | 原因 | 解法 |
|---|---|---|
| iOS 改了 Rust 但行为没变 | Xcode 没有 Run Script 调 Rust，用的旧 `.a` | `cd ios && just generate-stone` |
| iOS 链接报 `library not found for -lgemstone` | 从没跑过 generate-stone，或换了 configuration | `cd ios && just generate-stone`（Release 加 `BUILD_MODE=release`） |
| Android 模拟器上 `UnsatisfiedLinkError` | 只编了 arm ABI，缺 x86_64 | `GEMSTONE_ANDROID_ABIS=arm64-v8a,x86_64 ./gradlew ...` |
| JVM 单测报找不到库 | 没编宿主机版本 | `cd android && just build-gemstone-host` |
| 启动就 crash，提示 checksum / contract version | 绑定和原生库版本错位 | 全量重新生成，别手工拷文件 |
| Intel Mac 上 generate-stone 报错 | 脚本显式拒绝 x86_64 host | 用 Apple Silicon |
| 生成文件被手改后又被覆盖 | 生成产物不入库 / 每次重生成 | 改动写成 extension 放单独文件 |
| Xcode 里编译 Rust 时报 deployment target 冲突 | Xcode 注入了 `MACOSX_DEPLOYMENT_TARGET` 等变量 | `generate-stone.sh` 已经 `unset` 了，不要绕开脚本直接跑 cargo |
| 新增文案某语言没显示 | 只加了 `en.ftl` | 每个语言文件都要加，然后 `just localize` |
| Rust 侧设了 `X_CACHE_TTL` 但没缓存 | 平台没实现 `nativeCacheKey`，或 header 名字不一致 | 两端常量必须和 `gem_client::X_CACHE_TTL`（`x-gem-cache-ttl`）严格一致 |
| 服务端收到了奇怪的 `x-gem-cache-ttl` 头 | 平台发包前忘了 filter 掉 | 见 `asRequest()` / OkHttp builder 里的 header 过滤 |
| 同一请求被重试两次 | 平台侧和 Rust 侧都配了重试 | 明确归属：超时归平台，重试归 `gem_client::retry` |
| 请求偶发永久挂起 | 请求合并装饰器的取消路径没唤醒等待者 | 参考 `coalescing_provider.rs` 的 `RequestGuard::drop` |

---

## 11. 命令速查表

### 环境

```bash
just setup-git                       # git submodule
cd ios     && just bootstrap         # iOS 全套
cd android && just bootstrap         # Android 全套
cd android && just install-java      # Temurin JDK 17
```

### 生成

```bash
just generate                        # 全量：两端 models + stone
just generate-models                 # 只 typeshare（两端）
just generate-stone                  # 只 UniFFI（iOS）
just localize                        # 文案

cd ios     && just generate-stone    # iOS 绑定 + .a
BUILD_MODE=release just generate-stone   # Release 版
cd android && just generate-stone    # 显式触发（通常不需要）
```

### 构建 / 运行

```bash
just build                           # 两端全量

cd ios     && just build             # 快速 Debug 构建
cd ios     && just run               # 构建 + 装模拟器 + 启动
cd ios     && just build-package Primitives    # 单包
cd ios     && just archive-release   # Release 归档

cd android && just build             # assembleGoogleDebug
cd android && just run               # + adb install + logcat
cd android && just run-device        # 装真机
cd android && just release           # bundleGoogleRelease
```

### 测试

```bash
cd core    && just test <CRATE>
cd core    && just test-integration

cd ios     && just test [TARGET]
cd ios     && just test-integration  # UI tests，会 reset 模拟器

cd android && just test              # 含 build-gemstone-host
cd android && just test-integration  # connectedAndroidTest
```

### 质量

```bash
cd core    && just lint && just format      # clippy -D warnings + fmt
cd ios     && just lint && just format      # swiftlint + swiftformat
cd android && just lint
cd android && just mobsfscan                # 移动端安全扫描
```

### 环境变量

| 变量 | 作用 | 例子 |
|---|---|---|
| `BUILD_MODE` | debug / release | `BUILD_MODE=release just generate-stone` |
| `GEMSTONE_IOS_TARGETS` | 限定 iOS 架构 | `"aarch64-apple-ios-sim"` |
| `GEMSTONE_ANDROID_ABIS` | 限定 Android ABI | `"arm64-v8a,x86_64"` |
| `UNIT_TESTS` | Android 单测模式（只编 x86_64） | `true` |
| `SIMULATOR_NAME` | iOS 模拟器机型 | `"iPhone 16 Pro"` |
| `CONFIGURATION` | Xcode configuration | `Release` |

---

## 附：关键文件位置

| 用途 | 路径 |
|---|---|
| UniFFI crate | `core/gemstone/Cargo.toml`、`src/lib.rs` |
| UniFFI 绑定配置 | `core/gemstone/uniffi.toml` |
| 绑定生成命令 | `core/gemstone/justfile` |
| TypeShare 类型映射 | `core/typeshare.toml` |
| 代码生成器 | `core/bin/generate/src/main.rs` |
| iOS 生成脚本 | `ios/scripts/generate-stone.sh` |
| iOS SPM 包 | `ios/Packages/Gemstone/Package.swift` |
| iOS 链接配置 | `ios/Gem.xcodeproj/project.pbxproj`（`LIBRARY_SEARCH_PATHS`） |
| Android 壳模块 | `android/gemstone/build.gradle.kts` |
| Android 发布工程 | `core/gemstone/android/` |
| 下游示例工程 | `core/gemstone/tests/{ios,android}/GemTest` |
| 跨端开发流程 | `skills/new-feature-workflow.md` |

**网络委托相关（第 8 章）**

| 用途 | 路径 |
|---|---|
| `AlienProvider` trait 定义 | `core/gemstone/src/alien/provider.rs` |
| 请求/响应结构 | `core/gemstone/src/alien/target.rs` |
| 请求合并装饰器 | `core/gemstone/src/alien/coalescing_provider.rs` |
| 测试用 reqwest 实现 | `core/gemstone/src/alien/reqwest_provider.rs` |
| HTTP 客户端抽象 | `core/crates/gem_client/src/lib.rs` |
| Target → Client 适配 | `core/crates/gem_jsonrpc/src/rpc.rs` |
| 注入点（Rust） | `core/gemstone/src/gateway/mod.rs` |
| 存储委托 trait | `core/gemstone/src/gateway/preferences.rs` |
| iOS 实现 | `ios/Packages/NativeProviderService/Sources/` |
| iOS 装配 | `ios/Packages/Blockchain/Sources/Gateway/GatewayService.swift` |
| Android 实现 | `android/data/services/remote-gem/.../gemapi/NativeProvider.kt` |
| Android 装配 | `android/app/.../di/GatewayModule.kt` |

---

*本文档由 AI 辅助整理，内容基于仓库在 commit `820c415579` 时的实际配置。构建配置变更后请以仓库内的 `justfile` / `build.gradle.kts` / `generate-stone.sh` 为准。*
