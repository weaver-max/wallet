# 开发前的任务清单

> 用途：新钱包项目开工前的准备事项，按「能否并行」和「谁负责」组织
> 状态基准：2026-09-13，所有 ✅ 均为实测核实，非声称
> 相关：[脚本发布教程.md](脚本发布教程.md) · [gem开发core团队应该给ios和安卓提供什么.md](gem开发core团队应该给ios和安卓提供什么.md)

---

## 进度总览

| 板块 | 完成度 | 阻塞谁 |
|---|---|---|
| A. 仓库与代码托管 | ✅ **6/6 完成** | — |
| B. 本机工具链 | ⬜ 3/4 | 仅剩 Android |
| C. 凭据 | ⚠️ 已验证可用，**待轮换** | — |
| D. 代码前置改动 | ✅ **3/3 完成** | — |
| E. 首次发布验证 | ✅ **iOS 全通** / ⬜ Android 待 B2 | — |
| F. 待决策 | ⬜ 0/3 | 阻塞 M1 排期 |
| G. 上游依赖 | ⬜ 0/2 | **阻塞技术评审** |

> ✅ **iOS 制品链路已端到端打通**（2026-09-14 实测：发布 → 匿名下载 → SPM 解析 → 编译链接）。
> 🔴 **当前关键路径**：G（产品/UI 就绪）→ 技术评审 → M1 拆分。
> 剩余的 B2（Android NDK）可与 G 并行推进。

---

## A. 仓库与代码托管 ✅ 已完成

| # | 事项 | 状态 | 证据 |
|:---:|---|:---:|---|
| A1 | 建 `weaver-max/wallet`（core） | ✅ | 9,211 commits / 130 MB 已推送 |
| A2 | 建 `weaver-max/gemstone-swift` | ✅ | 6 commits 已推送 |
| A3 | `gemstone-swift` 设为 **public** | ✅ | API 实测 `visibility: public` |
| A4 | SSH key 配好，认证为 `weaver-max` | ✅ | `ssh -T` 返回 `Hi weaver-max!` |
| A5 | `scripts/release.env` 配置 | ✅ | CORE_REPO / SWIFT_REPO / SWIFT_REPO_GIT 已填 |
| A6 | 6 个发布脚本就位 | ✅ | 语法检查 5/5 + dry-run 实测通过 |

### remote 配置说明

```
origin    → git@github.com:weaver-max/wallet.git        你的
upstream  → https://github.com/gemwalletcom/wallet.git  gem 官方
```

同步上游用 `git fetch upstream`。

> ⚠️ 当前本地落后上游 **1675 个 commit**（基线 `820c415579`）。
> 所有文档里的源码行号引用基于此版本，pull 上游后可能对不上。

---

## B. 本机工具链 ⬜

`preflight.sh` 会逐项检查，缺什么会明确报出来。

| # | 事项 | 状态 | 阻塞 |
|:---:|---|:---:|---|
| B1 | Rust targets（5 个） | ✅ **已完成** | — |
| B2 | `ANDROID_HOME` + NDK | ⬜ 未设 | Android 侧 |
| B3 | 其他工具 | ✅ 已全 | — |
| B4 | 🔴 **完整 Xcode** | ⬜ **仅有 Command Line Tools** | **iOS 侧** |

### B1 — Rust targets ✅

```bash
rustup target add aarch64-linux-android armv7-linux-androideabi x86_64-linux-android \
                  aarch64-apple-ios aarch64-apple-ios-sim
```

已装：`aarch64-linux-android` · `armv7-linux-androideabi` · `x86_64-linux-android` ·
`aarch64-apple-ios` · `aarch64-apple-ios-sim`

### B3 — 其他工具 ✅

cargo · rustup · just · zip · curl · git · jq · gh · **cargo-ndk 4.1.2** · xcodebuild · swift

### B4 — Xcode 环境 ✅ 已完成（三小步，各卡了一次）

| 步骤 | 命令 | 卡点表现 |
|---|---|---|
| ① 装完整 Xcode | App Store / developer.apple.com | CLT 不含 iOS SDK，`cc-rs` 编译失败 |
| ② 切换开发目录 | `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` | 装了但没切，`xcrun` 仍找不到 SDK |
| ③ 装首次启动组件 | `sudo xcodebuild -runFirstLaunch` | CoreSimulator 缺失，`create-xcframework` 失败 |
| ④ 装模拟器 runtime | `xcodebuild -downloadPlatform iOS`（8.5 GB） | SDK 能编译但没 runtime，无法 `-destination iOS Simulator` |

**当前状态**：Xcode 26.6 · iOS SDK 26.5 · Simulator runtime 26.5 ✅

> 🔴 **SDK ≠ Simulator runtime，是两个东西。**
> SDK 用来编译（`.a` 能编出来靠它），runtime 用来跑/链接模拟器目标。
> ①②③ 装完只有 SDK，④ 才有 runtime。今天在这四步上各卡了一次。

> 💡 `preflight.sh` 已补 ①②③ 的检查（实际解析 SDK 路径）；
> ④ 由 `verify-release.sh` 在编译验证前检测，缺失时给出 `-downloadPlatform` 提示。

---

## C. 凭据 ⬜

| # | 事项 | 状态 |
|:---:|---|:---:|
| C1 | 建 classic PAT | ⚠️ 已有可用 token，**但需轮换** |
| C2 | 写入 `~/.zshrc` 持久化 | ⬜ |

> 🔴 **现用 token 必须吊销重建**，两个原因：
> ① 曾在对话中明文出现
> ② scope 过宽 —— 实测带着 `admin:enterprise` · `admin:org` · `delete_repo`，
>    发版只需 4 个。泄漏后可删仓库、改组织设置。
>
> 新 token 只勾：`repo` · `write:packages` · `read:packages` · `delete:packages`

### C1 — 建 token

```
https://github.com/settings/tokens → Tokens (classic) → Generate new token
```

🔴 **确认当前登录的是 `weaver-max`**（不是 `YanOctavian`）。

| scope | 用途 |
|---|---|
| `write:packages` | 发 AAR 到 GitHub Packages |
| `read:packages` | 下游拉包（可另发只读 token 给消费方） |
| `repo` | 建 Release、推 `gemstone-swift` |
| `delete:packages` | `rollback.sh` 撤回时需要 |

> ⚠️ **GitHub Packages 的 Maven registry 历史上只认 classic PAT**，fine-grained 支持有限。
> 先用 classic；组织策略强制 fine-grained 的话，用 `preflight.sh` 先验。
> **这一点未查最新官方文档，实测为准。**

### C2 — 环境变量

```bash
# ~/.zshrc
export GH_TOKEN="ghp_xxxxxxxxxxxx"
export GITHUB_ACTOR="weaver-max"
```

> ℹ️ SSH 只解决 `git push`。`gh release create` 和 Gradle 发 Maven 都走 HTTPS API，
> 仍需 token。

> ⚠️ `gh` CLI 当前登录的是 `YanOctavian`。`GH_TOKEN` 环境变量优先级更高会覆盖它，
> 但为免混淆，建议也跑一次 `gh auth login` 切到 `weaver-max`。

---

## D. 代码前置改动 ✅ 已完成

| # | 文件 | 改什么 | 状态 |
|:---:|---|---|:---:|
| D1 | `core/gemstone/uniffi.toml` | `generate_module_map` → `true` | ✅ |
| D2① | `core/gemstone/android/gemstone/build.gradle.kts` | 加 GitHub Packages `repositories` 块 | ✅ |
| D2② | 同上 | 版本号从 `core/Cargo.toml` 读，不再硬编码 `1.0.0` | ✅ |
| D3 | 示例工程 + `verify-release.sh` | 依赖坐标改为可传参，并补上 GitHub Packages 源 | ✅ |

`preflight.sh` 的「仓库前置改动」三项已全绿。

### D1 — 为什么需要

`binaryTarget` 要求 XCFramework **自带** `module.modulemap`。monorepo 里靠 SPM 的
`publicHeadersPath` 自动生成，跨仓库不行。不改的话 `package-xcframework.sh` 第一步就中止。

### D2 — 两个独立改动

**①** 原 `publishing` 块只有 `publications`（发什么）没有 `repositories`（发到哪），
Gradle 因此只生成 `publishXxxToMavenLocal`，推不了远端。补上后自动派生出
`publishReleasePublicationToGitHubPackagesRepository`。

仓库地址可用 `GITHUB_PACKAGES_REPO` 覆盖，默认 `weaver-max/wallet`。

**②** 原来硬编码 `?: "1.0.0"` 兜底，与 core 实际版本 `2.114.10` 脱节——**D3 那个 bug 就是这么产生的**。
改成读 `core/Cargo.toml`，`just bump` 后自动跟上。

### D3 — 比预想的多改了两处

原计划只改一行坐标，实际做的时候发现光改版本号不解决问题：

| 问题 | 处置 |
|---|---|
| 坐标 `gemstone:1.0.3` 与实际发布的 `gemstone-debug:1.0.0-debug` 对不上 | 改为 `${gemstoneVersion}`，默认读 `core/Cargo.toml`，`verify-release.sh` 传 `-PgemstoneVersion` |
| 示例工程 `settings.gradle` 只有 `mavenLocal()`，**根本拉不到远端制品** | 补上 GitHub Packages 源 |
| `verify-release.sh` 没传版本号 | 补 `-PgemstoneVersion="$VERSION"` 与 `GITHUB_PACKAGES_REPO` |

> ⚠️ **第二条是原计划漏掉的**：不补 GitHub Packages 源，`verify-release.sh` 声称的
> 「示例工程拉远端 AAR」实际只会从 `mavenLocal` 解析，**验收关等于摆设**。

> 💡 改动涉及**跟踪文件**，工作区已不干净。`release.sh` 会二次确认。
> **建议提交后再发版**，这样产物能追溯到确切 commit。

---

## E. 首次发布验证 — iOS ✅ 全通

| # | 步骤 | 状态 | 实测结果 |
|:---:|---|:---:|---|
| E0 | preflight | ✅ | iOS 部分全绿 |
| E1 | dry-run | ✅ | 六步输出正确，token 已打码 |
| E2 | Android 发本机验 ABI | ⬜ | 待 B2 |
| E3 | Android 发远端 | ⬜ | 待 B2 |
| E4 | iOS 本地打包 | ✅ | modulemap 实测为 `GemstoneFFI.modulemap` |
| E5 | iOS 发远端 | ✅ | tag + Release + 19 MB zip |
| E6 | 匿名下载 | ✅ | HTTP 200，checksum 双向一致 |
| E7 | **下游真实拉取** | ✅ | **SPM 解析 + 编译链接均通过** |
| E8 | 撤回测试版 | ✅ | `0.0.1-test` / `0.0.1-test2` 均已清理 |
| E9 | 正式发版 | ⬜ | 建议等 Android 侧就绪后双端一起发 |

### E4 — modulemap ✅ 实测确认

```
core/gemstone/generated/swift/
├── GemstoneFFI.h            135 KB
├── GemstoneFFI.modulemap    132 B     ← 实际文件名
└── gemstone.swift           757 KB / 24,331 行
```

D1（`generate_module_map = true`）确认生效，命中 `package-xcframework.sh` 第一个兼容分支。

### E7 — 🔴 抓到一个阻塞性 bug，值得单独记

**中文文件名让 SPM 无法解析整个包。**

```
error: the package at '/' cannot be accessed
  malformedResponse("unexpected tree entry '..."ios\345\246\202..."'")
```

git 对非 ASCII 文件名做八进制转义并加引号，SPM 的 git tree 解析器处理不了。

| 检查方式 | 能否发现 |
|---|:---:|
| curl 匿名下载 zip | ❌ 200 正常 |
| API 查 Release / tag / 资产 | ❌ 全绿 |
| 读 tag 下 `Package.swift` 核对 URL 与 checksum | ❌ 全绿 |
| `verify-release.sh` 原七项 | ❌ **全绿** |
| **真实 `swift package resolve`** | ✅ **立刻炸** |

已修：`ios如何调用这个库.md` → `iOS-Integration-Guide.md`（内容仍中文）。
`verify-release.sh` 已补真实 resolve + 编译检查，防止同类问题再次一路绿灯。

---

## F. 待决策 ⬜

| # | 决策 | 影响 | 状态 |
|:---:|---|---|:---:|
| F1 | 上不上 CI | 现有脚本已够手动发版；上 CI 需 macOS runner | ⬜ |
| F2 | 是否补齐 `verify` 等缺口接口 | 影响存量迁移的验证强度 | ⬜ |
| F3 | 是否同步上游 1675 commits | 影响文档行号引用与 M1 基线 | ⬜ |

### F1 — CI 取舍

| 发版频率 | 建议 |
|---|---|
| 每周 1 次以下 | ✅ 手动脚本足够，别为了 CI 而 CI |
| 每周多次 / 多人发布 | 上 CI |
| 有合规审计要求 | 必须上 CI（要可追溯） |

> 🔴 **GitHub 托管的 macOS runner 计费是 Linux 的 10 倍**，
> 且跨仓库推送必须用 PAT（内置 `GITHUB_TOKEN` 推不了 `gemstone-swift`）。
> **「Android 上 CI、iOS 手动」是很常见的现实选择。**

### F2 — 建议向 core 团队提的接口

| 接口 | 现状 | 用途 |
|---|---|---|
| `verify(keystoreId, password)` | Rust 有，**未导出 UniFFI** | 存量迁移的落库验证 |
| `change_password(...)` | 同上 | 两步法迁移的第二步 |
| `all_chains()` | 无 | 下游运行时拿全量链列表，免去猜字符串 |

三个都是包一层的事，几行代码。

---

## G. 上游依赖 🔴 当前关键路径

| # | 事项 | 负责 | 状态 |
|:---:|---|---|:---:|
| G1 | 产品完成需求定稿 | **产品** | ⬜ |
| G2 | UI 完成设计稿 | **UI** | ⬜ |

**技术评审原定 2026-09-08（周二），因 G1/G2 未就绪顺延至 09-14（周一）。**

技术侧材料已于 09-09 定稿待审（《钱包技术评审-20260908.md》1,572 行），**卡点完全在上游输入**。

### 连带影响

```
G1/G2 未就绪
   ↓
技术评审无法召开
   ↓
M1 任务拆分无法启动（DEO3-01）
   ↓
双端开工时间不确定
```

> 💡 **B/C/D/E 不依赖 G，可以现在就并行做完。**
> 这样评审一通过就能立刻进入实现阶段，不用再等环境准备。

---

## 推荐执行顺序

```
第 1 天（可立即开始，不等评审）
  └─ ✅ B1 装 Rust targets（已完成）
  └─ ✅ D1/D2/D3 代码改动 + 提交（已完成，commit 37fe95cf4f）
  └─ 🔴 B4 装完整 Xcode（10 GB，可后台下载）
  └─ C1/C2 建 token + 环境变量
  └─ E0 preflight 全绿

第 2 天
  └─ E1~E5 首次发布走通（先 iOS，Android 等 B2）
  └─ E6/E7 下游真实验证 ⭐ 最关键
  └─ E8 撤回测试版

第 3 天（B2 完成后）
  └─ Android 侧补齐验证
  └─ E9 正式发版 2.114.10

并行推进
  └─ G1/G2 催产品与 UI
  └─ F1/F2/F3 决策
```

---

## 阻塞与求助

| 问题 | 已卡多久 | 需要谁 |
|---|---|---|
| 技术评审延期（产品需求定稿 + UI 设计稿未完成） | 本周确认延期，0 天 | **产品 + UI** |

> 若下周一（09-14）G1/G2 仍未就绪，将开始产生实际排期损失，
> 需升级给技术负责人协调。

---

*本文档由 AI 辅助整理。A 板块 6 项、B3、D 板块 4 项的状态均为 2026-09-13 实测核实；
其余为待办项。环境变化后请重跑 `./scripts/preflight.sh` 核对。*
