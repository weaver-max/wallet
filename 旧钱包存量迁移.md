# 旧钱包存量迁移到 gem

> 面向：负责把自研旧钱包的存量密钥迁移到 gem keystore v4 的工程师
> 前置阅读：[gem私钥管理.md](gem私钥管理.md)
> 基线：仓库 commit `820c415579`，接口签名与行为均已对照源码实测

---

## 目录

1. [一句话结论](#1-一句话结论)
2. [迁移的正确模型](#2-迁移的正确模型)
3. [三个必须先定的前置决策](#3-三个必须先定的前置决策)
4. [接口清单：能用的和没有的](#4-接口清单能用的和没有的)
5. [完整流程](#5-完整流程)
6. [验证手段（重点）](#6-验证手段重点)
7. [幂等与回滚](#7-幂等与回滚)
8. [开工前的只读普查](#8-开工前的只读普查)
9. [风险清单](#9-风险清单)
10. [接口速查](#10-接口速查)

---

## 1. 一句话结论

**不要用 `migrate_v3`，用 `preview_import` + `create_store`。**

| | 适用场景 | 你的旧钱包 |
|---|---|:---:|
| `migrate_v3` | **WalletCore v3 keystore 文件**（`.json`，Trust Wallet 系） | ❌ 不适用 |
| `preview_import` + `create_store` | 任何来源的助记词 / 私钥明文 | ✅ **用这条** |

`migrate_v3` 读的是 WalletCore 定义的文件格式。自研旧钱包（Realm / FMDB / 自定义加密 / 密文当主键）它一概不认识。

> ⚠️ **"gem 有现成迁移工具"这个说法要降级。** gem 提供的是**密码学、派生和迁移测试的基础设施**；存量钱包迁移器仍需自研。**可复用的是骨架与验证模式，不是代码。**

---

## 2. 迁移的正确模型

### 核心 reframe

> ⭐ **旧库的行不是"要迁移的数据"，是"校验用的对照表"。**

```
旧库 iOS EVM 5 行  ──►  1 个 secret + 5 条核对项
旧库 安卓 BTC 3 行 ──►  1 个 secret + 3 条核对项
```

**只迁 secret（助记词或私钥），其余全部重新派生。** 地址、公钥、派生路径、账户列表，一律由 `gem_derivation` 重新算，旧库里的对应值只用来**比对**。

这样做的直接好处：「双端去重规则互相冲突」这类历史矛盾**自动消解**——因为你不再搬运那些冲突的派生结果，只搬运唯一的真相（secret），然后用统一的规则重新派生。

### 迁移边界

```
┌─ 阶段 A：你自己写（gem 帮不上）───────────────┐
│  读旧库 → 解密 → 得到助记词字符串 / 私钥       │
│  🔴 高风险，人工编写，技术负责人 Review        │
└──────────────────┬───────────────────────────┘
                   ▼  明文 secret（内存，Zeroizing）
┌─ 阶段 B：调 gem ─────────────────────────────┐
│  派生 → 核对 → 落盘 → 验证 → 写新库 → 标记    │
└──────────────────────────────────────────────┘
```

**阶段 A 是全自研的。** gem 不提供任何旧格式解密能力。

---

## 3. 三个必须先定的前置决策

这三件事不定，写出来的迁移器大概率要返工。

### ① `importType` 怎么判断 —— 最容易出错的地方

```rust
pub enum GemImportType {
    MulticoinPhrase { words: Vec<String>, chains: Vec<Chain> },
    SinglePhrase    { words: Vec<String>, chain: Chain },
    PrivateKey      { value: String, chain: Chain },
}
```

| 旧钱包实际情况 | 用哪个 | 迁移后 |
|---|---|---|
| 一套助记词派生多条链 | `MulticoinPhrase` | **1 个钱包** |
| 每条链一套独立助记词 | `SinglePhrase` × N | N 个钱包 |
| 用户当初导入的就是裸私钥 | `PrivateKey` | 1 个单链钱包 |

> 🔴 **如果旧库里"每链一份私钥"实际上是同一套助记词派生出来的，必须用一次 `MulticoinPhrase`，不能用 N 次 `PrivateKey`。**
>
> 后者会造出 N 个互不相关的钱包——用户打开 App 看到 N 个钱包，而且**再也合不回去**，因为 `privateKey_ethereum_0x...` 和 `multicoin_0x...` 是两个不同的 walletId，对应两个不同的 keystore 文件。
>
> **这个判断必须用数据确认（见 §8 普查），不能靠推测。**

### ② 密码用什么

`create_store` 要求传 password（`Vec<u8>`）。三种可能：

| 策略 | 说明 | 建议 |
|---|---|---|
| **新生成 256-bit 设备密钥** | 静默收口，完全不碰旧密码 | ⭐ **推荐** |
| 沿用用户旧密码 | 需要用户输入，迁移无法静默 | ❌ 不建议 |
| 让用户设新密码 | 有感知，转化率损失 | 延后到第二阶段 |

推荐两步法：

```
第一步（本期）：设备硬件密钥静默收口 —— 用户无感，立即止血
第二步（延后）：用户密码体系 —— 用户有感知，且不提供"沿用旧密码"选项
```

`create_store` 会拒绝空密码（`validate_v4_password`），v3 的空密码只为遗留兼容而接受。

### ③ 派生参数有没有历史分叉

如果旧钱包在某个版本改过派生路径（比如 EVM 从 `m/44'/60'/0'/0/0` 改成别的，或 BTC 换过地址格式），那么**同一套助记词在旧库里可能对应不止一组地址**。

这种情况下 `preview_import` 派生出来的地址会和部分旧记录对不上——**这不是 bug，是历史分叉**。必须在普查阶段发现，并决定：

- 分叉的账户按什么规则处理（迁移哪一组？两组都保留？）
- 是否需要 `SinglePhrase` 拆分

---

## 4. 接口清单：能用的和没有的

### ✅ UniFFI 上有的（`GemKeystore`）

| 方法 | 用途 | 迁移中的角色 |
|---|---|---|
| `new(base_dir)` | 构造 | 初始化 |
| **`preview_import(import)`** | 纯派生，不落盘、不要密码 | ⭐ **核对关卡的核心** |
| **`create_store(import, password)`** | 加密落盘 | ⭐ **写入** |
| **`add_accounts(keystore_id, password, chains)`** | 补链，内部会真解密 | ⭐ **助记词钱包的验证手段** |
| **`sign_auth(keystore_id, chain, hash, password)`** | 鉴权签名 | ⭐ **私钥钱包的验证手段** |
| `delete(keystore_id)` | 删除 | 回滚 |
| `keystore_id_for_wallet(wallet_id)` | 算 keystoreId | 定位文件 |
| `export_recovery_phrase` / `export_private_key` | 显式导出 | ⚠️ **不要用来做验证** |
| `migrate_v3(...)` | WalletCore v3 迁移 | ❌ 与你无关 |

### ❌ Rust 里有但 UniFFI 没导出的

`FileKeystore` 上存在，但 App 调不到：

```rust
pub fn verify(&self, keystore_id: &str, password: &[u8]) -> Result<StoredSecretMeta, KeystoreError>
pub fn get_meta(&self, keystore_id: &str) -> ...
pub fn list(&self) -> ...
pub fn change_password(&self, ...) -> ...
pub fn inspect_path(path: &Path) -> ...
pub fn verify_path(path: &Path, password: &[u8]) -> ...
```

> 🔴 **没有 FFI 层的 `verify` 是这次迁移最需要注意的缺口。**
>
> iOS 现在只能查文件在不在：
> ```swift
> // ios/Packages/Keystore/Sources/LocalKeystore.swift:233
> private func v4KeystoreExists(_ keystoreId: String) -> Bool {
>     let url = keystoreURL.appendingPathComponent("\(keystoreId).json")
>     return FileManager.default.fileExists(atPath: url.path)
> }
> ```
>
> 而 `KEYSTORE_V4.md` 自己就说了：
> > Metadata from `list`, `get_meta`, or `inspect` is **not proof** of migration success. Passworded `verify` or a real decrypt is required.
>
> **文件存在 ≠ 能解密。** 解决办法见 §6。

---

## 5. 完整流程

```
① 解密旧库                 ← 自研
② preview_import           ← 派生，不落盘
③ 地址核对                 ← 🔴 关卡，不过就停
④ create_store             ← 落盘，幂等
⑤ 解密验证 + 二次地址核对   ← 🔴 关卡，不过就回滚
⑥ 写新 wallets / accounts 表
⑦ 标记旧记录已迁移         ← 只标记，先不删
```

### 伪代码

```
for 旧钱包 in 旧库.所有钱包():

    # ── ① 阶段 A：自研解密（高风险，人工编写）──
    secret   = 解密旧库(旧钱包)              # 助记词字符串 或 私钥
    旧地址集 = 旧库.查该钱包所有地址(旧钱包)

    # ── ② 派生预览：不落盘、不要密码 ──
    importType = 判断导入类型(旧钱包)         # ← 见 §3①，必须用数据确认
    preview    = keystore.previewImport(importType)

    # ── ③ 关卡一：地址核对 ──
    if not 地址全匹配(preview.accounts, 旧地址集):
        记录失败(旧钱包, preview.accounts, 旧地址集)
        continue                             # 🔴 什么都不写，什么都不删

    # ── ④ 落盘（幂等，崩溃可重跑）──
    password = 取或建设备密钥()               # iOS Keychain / Android Tink
    stored   = keystore.createStore(importType, password)
    # stored = { walletId, walletType, keystoreId, accounts }

    # ── ⑤ 关卡二：解密验证 + 二次核对 ──
    if not 验证可解密并核对(stored, password, 旧地址集):   # 见 §6
        keystore.delete(stored.keystoreId)   # 回滚
        记录失败(旧钱包)
        continue

    # ── ⑥ 写新库 ──
    新库.写钱包(stored.walletId, stored.walletType, 旧钱包.名称, 旧钱包.排序)
    新库.写账户(stored.walletId, stored.accounts)

    # ── ⑦ 只标记，不删 ──
    旧库.标记已迁移(旧钱包)
```

### 关于 `chains` 参数

`MulticoinPhrase { words, chains }` 里的 `chains` 传你希望启用的链。

Rust 内部会**强制补上 Ethereum** 用来算 walletId，算完再从返回的 accounts 里过滤掉：

```rust
// core/gemstone/src/keystore/keystore.rs:216
let mut chains = requested_chains.clone();
if !chains.contains(&wallet_id_chain) {
    chains.push(wallet_id_chain);            // 强制补 ETH
}
...
let accounts = derived_accounts.into_iter().filter(|a| requested_chains.contains(&a.chain)).collect();
//                                                     ↑ 再过滤掉
```

所以你不传 ETH 也不影响 walletId 口径，可以放心只传实际需要的链。

---

## 6. 验证手段（重点）

因为 FFI 上没有 `verify`，需要用别的调用达到同等效果。

### 按钱包类型选

| 钱包类型 | 验证方法 | 为什么 |
|---|---|---|
| **助记词**（`MulticoinPhrase` / `SinglePhrase`） | ⭐ **`add_accounts`** | 内部会**真解密**助记词，返回 accounts（只有地址和公钥，无密钥材料）。**既验证可解密，又能拿地址做二次核对** |
| **私钥**（`PrivateKey`） | ⭐ **`sign_auth`** | `add_accounts` **不支持私钥钱包**，会显式报错 |

### 助记词钱包：`add_accounts`

```rust
// core/gemstone/src/keystore/keystore.rs:76
pub fn add_accounts(&self, keystore_id: String, password: Vec<u8>, chains: Vec<Chain>) -> Result<Vec<GemKeystoreAccount>, GemstoneError> {
    let phrase = match self.inner.decrypt_mnemonic_with_meta(&keystore_id, &password) {
        Ok((_meta, phrase)) => phrase,                              // ← 真解密
        Err(KeystoreError::CorruptFile(msg)) if msg == "stored secret is not a mnemonic" => {
            return Err(GemstoneError::from("add_accounts does not support private-key wallets"));
        }
        Err(error) => return Err(error.into()),
    };
    Ok(derive_accounts_from_mnemonic(&phrase, chains)?.into_iter().map(GemKeystoreAccount::from).collect())
}
```

**双重校验，基本免费：**

```
add_accounts(keystoreId, password, 目标链)
    ├─ 没抛错          ⇒ 文件可解密、密码正确 ✅
    └─ 地址再比对一次  ⇒ 内容确实是那个 secret ✅
```

### 私钥钱包：`sign_auth`

```rust
pub fn sign_auth(&self, keystore_id: String, chain: Chain, hash: Vec<u8>, password: Vec<u8>) -> Result<String, GemstoneError>
```

底层走 `load_private_key`，两种 secret kind 都支持：

```rust
// keystore.rs:158
fn load_private_key(&self, keystore_id: &str, chain: Chain, password: &[u8]) -> Result<Zeroizing<Vec<u8>>, GemstoneError> {
    match self.inner.decrypt_mnemonic(keystore_id, password) {
        Ok(phrase) => Ok(derive_private_key_from_mnemonic(&phrase, chain)?),
        Err(KeystoreError::CorruptFile(msg)) if msg == "stored secret is not a mnemonic" =>
            Ok(self.inner.decrypt_private_key(keystore_id, password)?),   // ← 私钥分支
        Err(error) => Err(error.into()),
    }
}
```

签一个固定 dummy hash，能返回签名就证明解密成功，**且不返回任何密钥材料**。

### 🔴 不要用 `export_recovery_phrase` 做验证

它会把助记词明文拉回 App 内存。为了做一次验证付这个代价不值——违背整个架构"私钥不跨 FFI"的设计初衷。它只应该出现在用户明确点击"查看助记词"的路径上。

### 可选：给 gemstone 加 `verify` 导出

最干净的方案。`FileKeystore::verify` 已经存在，包一层即可：

```rust
#[uniffi::export]
impl GemKeystore {
    pub fn verify(&self, keystore_id: String, password: Vec<u8>) -> Result<bool, GemstoneError> {
        let password = Zeroizing::new(password);
        Ok(self.inner.verify(&keystore_id, &password).is_ok())
    }
}
```

代价是 UniFFI 接口变更，要重新生成两端绑定（iOS 记得 `just ios generate-stone`）。**如果迁移是长期工程，建议加；如果只做一次性迁移，用 §6 的现有手段即可。**

---

## 7. 幂等与回滚

### `create_store` 天然幂等——但要理解它的确切语义

```rust
// core/crates/gem_keystore/src/storage/file_keystore.rs:201
fn import_payload_unlocked(&self, kind, payload, password, keystore_id) -> Result<StoredSecretMeta, KeystoreError> {
    let id = match keystore_id { Some(k) => KeystoreId::parse(&k)?, None => KeystoreId::new() };
    if self.path_for_id(&id).exists() {
        return self.verify_unlocked(id.as_str(), password);      // ← 不覆盖，改为验证
    }
    let body = self.encrypt_payload(&kind, payload, password, Some(id.clone()))?;
    self.write_new_file(&id, &body, false)?;
    self.get_meta_unlocked(id.as_str())?.ok_or(KeystoreError::NotFound)
}
```

三条重要性质：

| 性质 | 说明 |
|---|---|
| **文件已存在时不覆盖** | 改走 `verify_unlocked`，用密码验证后返回现有 meta |
| **密码不对会失败而不是重写** | ⭐ 这是好事——防止用错密码把文件重写掉，导致原密钥永久丢失 |
| **首次写入不做解密验证** | 🔴 只写不验。所以 §6 的显式验证步骤**不能省** |

> ⭐ 结论：**崩溃后重跑同一个钱包是安全的**，第二次调用会退化成一次密码验证。这满足「崩溃可重跑（幂等）」那条要求。

### 回滚

```
keystore.delete(keystoreId)     // 删 v4 文件
```

配合「只增不删」原则：

| 阶段 | 旧数据 | 新数据 |
|---|---|---|
| 迁移中 | 保留 | 写入 |
| 灰度观察期 | **保留**，仅标记已迁移 | 生效 |
| 观察期结束 | 统一清理 | — |

**任何时候都要能回滚到"只用旧数据"的状态。** 这意味着灰度期内旧读取路径必须保持可用。

---

## 8. 开工前的只读普查

> ⭐ **这是整个迁移里性价比最高的一步：零写入、零风险，但能提前暴露绝大多数问题。**

对所有存量钱包跑一遍**只调 `preview_import`** 的脚本（不调 `create_store`，不写任何文件）：

```
for 旧钱包 in 旧库.所有钱包():
    secret     = 解密旧库(旧钱包)
    importType = 判断导入类型(旧钱包)
    preview    = keystore.previewImport(importType)     # 纯内存，不落盘

    统计.记录(
        钱包 = 旧钱包.id,
        判定类型 = importType,
        派生地址 = preview.accounts.map(地址),
        旧库地址 = 旧库.查地址(旧钱包),
        匹配 = 集合比较(派生地址, 旧库地址),
    )

输出：匹配率、不匹配明细、类型分布
```

### 要看什么

| 指标 | 期望 | 不达标说明什么 |
|---|---|---|
| **地址匹配率** | **100%** | 类型判断错了，或派生参数有历史分叉 |
| 类型分布 | 与产品认知一致 | 若出现大量 `PrivateKey`，要确认是不是应该合并成 `MulticoinPhrase` |
| 每钱包链数 | 与旧库账户行数一致 | 少了说明 `chains` 传漏，多了说明旧库有死链 |
| 解密失败数 | **0** | 阶段 A 的解密器有缺陷 |

**匹配率不是 100% 就不要开工。** 这正好对应技术评审 §6.9 第 3 条「安卓侧按调用链复核（派生参数是否有历史分叉）」——普查是那条的执行手段。

---

## 9. 风险清单

| # | 风险 | 处置 |
|:---:|---|---|
| **1** | 🔴 **`importType` 判断错** | 一套助记词被拆成 N 个 `PrivateKey` 钱包后**无法合并**。必须用 §8 普查确认，不能推测 |
| **2** | 🔴 **阶段 A 解密器属最高风险模块** | 按规范：**人工编写，AI 不参与，技术负责人 Review**。AI 可协助写测试用例和核对清单 |
| **3** | 🔴 **地址核对必须是关卡，不是日志** | 不匹配就停止该钱包迁移，不能"记录一下继续跑" |
| **4** | 🔴 **FFI 无 `verify`，文件存在 ≠ 迁移成功** | 用 §6 的 `add_accounts` / `sign_auth` 显式验证 |
| **5** | ⚠️ **首次 `create_store` 不做解密验证** | 验证步骤不能省 |
| **6** | ⚠️ **地址大小写敏感** | ETH 地址的 EIP-55 混合大小写直接进 walletId → 影响 keystoreId。**核对时要按规范化形式比较**，不要用大小写不敏感的比较掩盖问题 |
| **7** | ⚠️ **派生路径历史分叉** | 普查阶段发现，产品侧定规则 |
| **8** | ⚠️ **secret 在阶段 A 到 B 之间是明文** | 全程 `Zeroizing` 持有，禁止 log / print / 崩溃上报 / 快照。**这段内存是整个迁移最脆弱的窗口** |
| **9** | ⚠️ **不要用 `export_recovery_phrase` 验证** | 会把明文拉回 App 内存 |
| **10** | ⚠️ **迁移进度标记要幂等** | 用「旧记录是否已标记」判断，不要用计数器或游标 |

### 关于 secret 明文窗口

第 8 条值得展开。整个架构的红线是"私钥不跨 FFI"，但**迁移是唯一必须打破它的场景**——你必须在 App 侧持有明文 secret，才能喂给 `create_store`。

最小化这个窗口：

- secret 从解密出来到传入 `create_store`，**中间不要经过任何日志、缓存、可观测性组件**
- 用平台的安全内存类型持有（Swift 用 `Data` + 显式 `resetBytes`，Kotlin 用 `CharArray`/`ByteArray` + `fill(0)`）
- **不要批量解密**——一个钱包解密、迁移、擦除，再处理下一个。不要先把所有 secret 解出来放数组里
- 迁移代码路径上禁用崩溃上报的内存快照

---

## 10. 接口速查

```
// 构造
GemKeystore(baseDir: String)

// 导入类型
GemImportType.MulticoinPhrase(words: [String], chains: [Chain])
GemImportType.SinglePhrase(words: [String], chain: Chain)
GemImportType.PrivateKey(value: String, chain: Chain)

// ② 派生预览（不落盘、不要密码）
previewImport(import) -> GemWalletImport {
    walletId: String
    walletType: GemWalletType
    accounts: [GemKeystoreAccount { chain, address, derivationPath, publicKey }]
}

// ④ 落盘（幂等；文件已存在则退化为密码验证）
createStore(import, password: [UInt8]) -> GemStoredWallet {
    walletId: String
    walletType: GemWalletType
    keystoreId: String
    accounts: [GemKeystoreAccount]
}

// ⑤ 验证 —— 助记词钱包
addAccounts(keystoreId, password, chains) -> [GemKeystoreAccount]
// 验证 —— 私钥钱包（addAccounts 对私钥钱包会报错）
signAuth(keystoreId, chain, hash, password) -> String

// 回滚
delete(keystoreId) -> Bool

// 定位文件
keystoreIdForWallet(walletId) -> String
```

### 关键文件

| 用途 | 路径 |
|---|---|
| UniFFI 边界实现 | `core/gemstone/src/keystore/keystore.rs` |
| 返回类型定义 | `core/gemstone/src/keystore/types.rs` |
| 落盘 / 幂等逻辑 | `core/crates/gem_keystore/src/storage/file_keystore.rs` |
| walletId 派生 | `core/crates/gem_derivation/src/wallet_id.rs` |
| keystoreId 派生 | `core/crates/gem_keystore/src/id.rs` |
| 权威格式文档 | `core/docs/KEYSTORE_V4.md` |
| iOS keystore 入口 | `ios/Packages/Keystore/Sources/LocalKeystore.swift` |
| Android keystore 包装 | `android/blockchain/.../operators/gemstone/GemKeystoreAccess.kt` |

---

## 附：v3 迁移的可复用模式

虽然 `migrate_v3` 本身用不上，但它的**骨架值得照抄**：

```
先写新格式 → 用结果反推 ID 比对 → 不一致就删新文件保旧文件
           → 验证通过才删旧文件 → 幂等重跑
```

它的内容级校验尤其值得学：

```rust
// core/gemstone/src/keystore/keystore.rs:194
let derived = derive_wallet_id_from_account(&account, wallet_type)?;
if derived != expected {
    return Err(...(
        "migrated secret does not derive the wallet id: \
         expected_wallet_id={expected}, derived_wallet_id={derived}, \
         derived_chain={}, derived_address={}"
    ));
}
```

**解出来的密钥必须能重新派生出同一个 walletId，否则拒绝迁移。** 这比"文件写成功了"强得多——证明的是密钥内容对，不只是文件搬过去了。

另一个值得复用的模式：**用"旧文件是否还存在"当迁移状态标记**，而不是另建状态表。状态和事实是同一个东西，不可能不同步。

gem 的 v3 迁移有 6 个测试覆盖空盐 / 坏文件 / 派生分叉等分支（`core/gemstone/src/keystore/keystore.rs` 的 `migration_tests` 模块）。**这三件最容易写错的事它已经做完了，照着写测试比照着写代码收益更大。**

---

*本文档由 AI 辅助整理，接口签名与行为基于 commit `820c415579` 实测复核。密钥相关代码变更后请以源码与 `core/docs/KEYSTORE_V4.md` 为准。*
