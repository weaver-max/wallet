# 项目结构

> 面向：需要在这个 monorepo 里找东西、或判断某个改动该落在哪一层的工程师
> 内容：每个目录、每个 crate、每个双端模块负责什么，谁依赖谁，以及"我要改 X 该去哪"
> 快照：`8c5e21bde4`（2026-09-15）。行数是实测值，用于建立规模感，不是精确指标

已有的英文说明：[skills/project-overview.md](skills/project-overview.md)（顶层三句话）、[core/skills/project-structure.md](core/skills/project-structure.md)（Core 侧，**部分已过时**，见 [附录 B](#附录-b与已有文档的差异)）。本文按当前代码实测重写，覆盖全部模块。

对照篇：[基于gem-core的ios和安卓开发教程.md](基于gem-core的ios和安卓开发教程.md)（构建链路怎么串起来） · [如何添加新链（包括EVM兼容）.md](如何添加新链（包括EVM兼容）.md)（接链时要动哪些模块）

---

## 目录

1. [全景](#1-全景)
2. [顶层目录](#2-顶层目录)
3. [core/ —— Rust 共享内核](#3-core--rust-共享内核)
4. [ios/ —— SwiftUI 应用](#4-ios--swiftui-应用)
5. [android/ —— Compose 应用](#5-android--compose-应用)
6. [其余顶层目录](#6-其余顶层目录)
7. [依赖流向](#7-依赖流向)
8. [速查：我要改 X，该去哪](#8-速查我要改-x该去哪)

---

## 1. 全景

**一句话：所有业务逻辑用 Rust 写一遍，iOS 和 Android 只写 UI。**

```
                    ┌─────────────────────────────────────────┐
                    │  core/  ── Rust，203k 行，65 个 crate    │
                    │                                         │
                    │   crates/    业务逻辑 + 20 条链的实现     │
                    │   gemstone/  FFI 边界，双端唯一入口       │
                    │   apps/      后端服务（api/daemon/        │
                    │              dynode/egress）             │
                    └─────────────────────────────────────────┘
                              │                    │
                   TypeShare  │                    │  UniFFI
                   （纯数据模型）│                    │（可调用的函数）
                              ▼                    ▼
                  ┌───────────────────┐  ┌───────────────────┐
                  │  ios/             │  │  android/         │
                  │  21 个 SPM 包      │  │  ~120 个 Gradle    │
                  │  23 个 Feature     │  │  模块              │
                  │  SwiftUI + MVVM   │  │  Compose + Hilt   │
                  └───────────────────┘  └───────────────────┘
```

两条管线的区别（详见[开发教程](基于gem-core的ios和安卓开发教程.md)）：

| 管线 | 产出 | 过 FFI 吗 | 用途 |
|---|---|:---:|---|
| **TypeShare** | `Primitives.swift` / `primitives/*.kt` | ❌ | 纯数据模型，可直接入库 |
| **UniFFI** | `Gemstone.swift` + `libgemstone.a` / `*.kt` + `libgemstone.so` | ✅ | 可调用的函数、可回调的接口 |

### 规模速览

| 区域 | 规模 |
|---|---|
| `core/crates/` | 65 个 crate |
| `core/` 全部 Rust | 203,383 行 |
| `core/gemstone/src/` | 9,257 行（FFI 边界层） |
| `ios/Packages/` | 21 个 SPM 包 |
| `ios/Features/` | 23 个业务域 |
| `android/` | ~120 个 Gradle 模块 |

---

## 2. 顶层目录

| 目录 / 文件 | 职责 |
|---|---|
| `core/` | Rust 共享内核 + 后端服务，见 [§3](#3-core--rust-共享内核) |
| `ios/` | SwiftUI 应用，见 [§4](#4-ios--swiftui-应用) |
| `android/` | Compose 应用，见 [§5](#5-android--compose-应用) |
| `localization/` | **移动端文案的唯一源头**（Fluent `.ftl`），见 [§6.1](#61-localization) |
| `maestro/` | Maestro UI 自动化测试流程 |
| `scripts/` | 发布链路脚本（bump / release / rollback / verify） |
| `build/` | iOS 用的 `GemstoneFFI.xcframework` 构建产物 |
| `docs/` | 跨端子系统设计文档（深链、支付、订阅、swap） |
| `skills/` | 给 AI 协作者的仓库规范（架构、安全、代码审查、发布流程） |
| `justfile` | 根任务入口（`just generate` / `just localize` 等） |
| `rust-toolchain.toml` | 锁定 Rust 工具链版本 |
| `AGENTS.md` / `CLAUDE.md` | 协作总则（`CLAUDE.md` 是指向 `AGENTS.md` 的符号链接） |

---

## 3. core/ —— Rust 共享内核

```
core/
├── crates/       65 个库 crate：业务逻辑 + 链实现
├── gemstone/     FFI 边界层，双端唯一入口（9,257 行）
├── apps/         4 个后端服务
├── bin/          6 个工具二进制
├── docs/         Core 子系统设计文档
├── skills/       Core 侧协作规范
├── Settings.yaml 后端配置（RPC 节点、第三方 provider key）
├── Cargo.toml    workspace 定义
├── typeshare.toml TypeShare 生成配置
└── diesel.toml   ORM 配置
```

### 3.1 地基层 —— 几乎所有 crate 都依赖它

| crate | 行数 | 职责 |
|---|---:|---|
| **`primitives`** | 20,268 | **整个仓库的类型中心**。`Chain` / `Asset` / `Transaction` / `Wallet` 等全部领域模型，链配置表（`chain_config.rs`），TypeShare 标注就打在这里。**改动会波及所有东西** |
| `chain_traits` | 797 | 每条链要实现的契约（`ChainBalances` / `ChainTransactions` / `ChainToken` / `ChainTransactionLoad` …共 15 个 trait），多数方法带默认实现 |
| `chain_primitives` | 338 | 链层通用小工具：余额差异计算、token id 格式化 |
| `gem_client` | 715 | **HTTP 客户端抽象**。两个实现：`ReqwestClient`（后端用）和 `AlienProvider`（移动端用，网络请求委托给平台） |
| `gem_jsonrpc` | 804 | 自研 JSON-RPC 客户端（替掉了外部 alloy 依赖），含 gRPC 和 alien 适配 |
| `serde_serializers` | 793 | 跨 crate 共用的 Serde 自定义序列化器（BigInt / BigUint / Duration / 字符串数字…） |

### 3.2 链实现 —— `gem_*`

每条链一个 crate，内部结构统一：`address.rs`（地址编解码）+ `models/`（RPC 模型）+ `rpc/`（调用）+ `provider/`（实现 `chain_traits`）+ `signer/`（签名）。

| crate | 行数 | 覆盖的链 |
|---|---:|---|
| `gem_evm` | 13,489 | 全部 EVM 链（30 条：Ethereum / BSC / Polygon / Arbitrum / Base / zkSync …） |
| `gem_hypercore` | 8,032 | HyperCore（永续合约交易） |
| `gem_sui` | 8,009 | Sui |
| `gem_tron` | 7,126 | Tron |
| `gem_solana` | 6,114 | Solana（含 Metaplex NFT、token account） |
| `gem_ton` | 5,104 | TON（含 TVM） |
| `gem_bitcoin` | 3,818 | Bitcoin / Bitcoin Cash / Litecoin / Dogecoin / Zcash |
| `gem_aptos` | 3,245 | Aptos（含 Move 模块） |
| `gem_cosmos` | 3,121 | Cosmos / Osmosis / Celestia / Injective / Sei / Noble / THORChain / MayaChain |
| `gem_near` | 2,756 | NEAR |
| `gem_xrp` | 2,478 | XRP Ledger |
| `gem_cardano` | 1,912 | Cardano |
| `gem_stellar` | 1,863 | Stellar |
| `gem_polkadot` | 1,672 | Polkadot |
| `gem_algorand` | 1,426 | Algorand |
| `gem_tempo` | 1,029 | Tempo（EVM 家族但有独立 provider / signer） |

链的**补充实现**（不是完整链，只补某条链缺的能力）：

| crate | 行数 | 职责 |
|---|---:|---|
| `gem_bsc` | 259 | BSC 的 StakeHub 质押合约读取 |
| `gem_optimism` | 135 | OP Stack 的 L1 gas oracle |

### 3.3 链数据源 —— 第三方索引器

| crate | 行数 | 职责 |
|---|---:|---|
| `gem_alchemy` | 379 | Alchemy：地址历史、代币余额、NFT。含各链的 network slug 映射（`url.rs`） |
| `gem_ankr` | 213 | Ankr Advanced API：地址历史、代币转账 |
| `gem_blockscout` | 164 | Blockscout PRO 多链网关：交易、代币转账、代币余额 |

> 这三家在 `gem_evm/src/rpc/indexer/` 里按顺序 failover，**同时提供"交易记录"和"代币自动发现"**。某条链落到 `return None` 就意味着这两个功能静默失效——细节见[接链文档 §3.3](如何添加新链（包括EVM兼容）.md)。

### 3.4 密钥与签名 —— 高风险区

| crate | 行数 | 职责 |
|---|---:|---|
| `gem_keystore` | 2,058 | **Keystore v4 文件格式**：助记词/私钥的加密存储、v3 迁移、账户公钥。私钥不出这一层 |
| `gem_derivation` | 1,121 | HD 派生：助记词 → 账户，私钥导入，钱包 id 推导。四种 scheme（Bip44 / Bip84 / Slip10 / Cardano） |
| `signer` | 1,113 | 签名算法层：Ed25519 / secp256k1 / EIP-712 类型化数据哈希、私钥编解码 |
| `gem_crypto` | 131 | 密码学小工具：哈希、PBKDF、安全随机数、常数时间比较 |
| `gem_hash` | 155 | 哈希算法集：keccak / sha2 / sha3 / blake2 |
| `gem_encoding` | 715 | 编码：base32 / base64 / protobuf |
| `gem_auth` | 503 | 设备认证：Ed25519 请求签名、JWT 设备令牌 |

⚠️ 这一区改动前必读 [skills/security.md](skills/security.md) 和 [core/docs/KEYSTORE_V4.md](core/docs/KEYSTORE_V4.md)。**私钥不跨 FFI**——签名在 Rust 内完成。

### 3.5 行情与价格

| crate | 行数 | 职责 |
|---|---:|---|
| `prices` | 1,669 | 价格 provider 聚合层（CoinGecko / DefiLlama / Jupiter / Pyth），含各链原生币的 feed 映射 |
| `pricer` | 592 | 对外的价格业务客户端：现价、K 线、市场数据、价格提醒规则 |
| `coingecko` | 720 | CoinGecko API 客户端 + 链/平台 id 双向映射 |
| `coinmarketcap` | 330 | CoinMarketCap API 客户端 + 平台映射 |
| `number_formatter` | 1,157 | 数字与货币格式化、加密↔法币换算、金额输入建议 |

### 3.6 交易增强

| crate | 行数 | 职责 |
|---|---:|---|
| **`swapper`** | 26,369 | **仓库最大的 crate**。全部 swap 集成：Uniswap V3/V4、Across、Chainflip、Cetus、OKX、Relay、Mayan、NEAR Intents、Jupiter…含跨链桥、手续费、推荐返佣 |
| `simulation` | 1,034 | 交易模拟（目前只有 EVM） |
| `defi` | 767 | DeFi 协议数据聚合（provider 工厂 + 多 provider 客户端） |
| `yielder` | 578 | 生息 / Earn 产品聚合（`EarnProvider`） |
| `jupiter` | 288 | Jupiter（Solana DEX 聚合器）API 客户端 |

### 3.7 业务服务

| crate | 行数 | 职责 |
|---|---:|---|
| `fiat` | 7,053 | 法币出入金：MoonPay / Transak / Mercuryo / Paybis / Banxa 等 provider，含 HMAC 签名、IP 检查 |
| `gem_rewards` | 2,319 | 积分与推荐：兑换、用户名、IP 风控（AbuseIPDB / ipapi） |
| `name_resolver` | 2,130 | 域名解析：ENS / SNS / AllDomains / DID / Aptos / Base 等 |
| `nft` | 2,117 | NFT：provider 工厂 + 多 provider 聚合 + 业务客户端 |
| `gem_wallet_connect` | 2,055 | WalletConnect：会话、请求分发、响应构造、签名类型校验 |
| `support` | 1,430 | 客服：Chatwoot 客户端 + webhook 校验 + 消息 markdown 解析 |
| `security_provider` | 601 | 地址/代币安全扫描（诈骗与钓鱼识别） |
| `lists` | 173 | 资产榜单数据（目前 CoinGecko provider） |
| `portfolio` | 111 | 资产组合总值客户端 |
| `in_app_notifications` | 127 | 把后端通知数据映射成应用内通知列表项（纯映射 + 本地化） |
| `api_connector` | 221 | 后端连接件：Pusher 推送客户端 + 静态资源客户端 |

### 3.8 存储与基础设施

| crate | 行数 | 职责 |
|---|---:|---|
| `storage` | 11,195 | **数据层**：Diesel ORM 模型、schema、迁移、repositories（后端 PostgreSQL） |
| `streamer` | 1,468 | RabbitMQ 消息流：生产者、消费者、队列、交换机 |
| `settings` | 524 | 后端配置管理（对应 `Settings.yaml`） |
| `settings_chain` | 622 | **链 provider 装配**：把配置 + 客户端 + 索引器组装成可用的 `ChainProviders`。接新链的关键注册点 |
| `cacher` | 579 | 缓存层（Redis） |
| `localizer` | 370 | 后端/Core 侧 i18n（Fluent，20+ 语言，与移动端文案分开） |
| `search_index` | 370 | 搜索索引模型（Meilisearch） |
| `job_runner` | 217 | 后台任务调度框架 |
| `tracing` | 186 | 日志与链路追踪 |
| `metrics` | 39 | Prometheus 指标注册表 |

### 3.9 gemstone/ —— FFI 边界层

**双端调用 Core 的唯一入口。** 9,257 行，全部是"把 crates 的能力包装成 UniFFI 能导出的形状"。

```
core/gemstone/src/
├── alien/          平台能力回调（网络请求委托给 iOS/Android）
├── gateway/        链 provider 工厂（chain_factory.rs：双端用的 RPC-only 版本）
├── signer/         签名分派（chain.rs：ChainType → ChainSigner）
├── keystore/       keystore 操作导出
├── wallet_connect/ WalletConnect 导出
├── config/         链配置查询（双端从这里读 ChainConfig）
├── network/        网络层
├── message/        消息签名
├── models/         FFI 模型
├── api_client/     后端 API 客户端
├── block_explorer/ 浏览器 URL 构造
├── address.rs      地址校验分派
├── payment.rs      支付 URI 解码
├── deeplink.rs     深链解析
├── fee.rs          手续费
└── ...
```

关键约定：**外部模型一律用 UniFFI remote types**，不要在这里重新定义 `primitives` 已有的类型。

产物：
- iOS：`Gemstone.swift` + `build/GemstoneFFI.xcframework`
- Android：`libgemstone.so` + `uniffi/*.kt`（Gradle 自动生成）

### 3.10 apps/ —— 后端服务

| 服务 | 行数 | 职责 |
|---|---:|---|
| `daemon` | 10,949 | 后台任务集：资产更新、垃圾币识别、推送通知、交易索引、各类 consumer |
| `dynode` | 6,266 | **动态节点代理**：链 RPC 的统一出口，带缓存、健康检查、指标。`chains.yml` 配置每条链的上游节点 |
| `api` | 6,122 | REST API + WebSocket 价格推流（Rocket 框架） |
| `egress` | 1,523 | **出站 API 网关**：按 caller key 鉴权 + 路由到第三方服务 + 多端点选择 + 失败冷却 + 可选出站代理。作用是把第三方凭据集中在服务端，App 不直接持有 |

### 3.11 bin/ —— 工具二进制

| 二进制 | 职责 |
|---|---|
| `uniffi-bindgen` | 生成 iOS / Android 的 UniFFI 绑定 |
| `generate` | 代码生成（TypeShare 模型等） |
| `nodecheck` | 批量检查各链 RPC 节点健康度 |
| `gas-bench` | gas 基准测试 |
| `img-downloader` | 资产图标批量下载 |
| `cli` | 开发调试命令行 |

---

## 4. ios/ —— SwiftUI 应用

```
ios/
├── Gem/              宿主 App（入口、导航、Scenes、Services）
├── Features/         23 个业务域（UI + ViewModel）
├── Packages/         21 个 SPM 包（可复用层）
├── GemPriceWidget/   价格小组件
├── GemTests/         单元测试
├── Submodules/       第三方源码依赖（BigInt / GRDB / GRDBQuery / reown-swift）
├── Gem.xcodeproj     Xcode 工程
└── swiftgen.yml      SwiftGen 配置（生成图片/文案访问器）
```

### 4.1 Packages/ —— 分层

**生成层**（不要手改）：

| 包 | 行数 | 职责 |
|---|---:|---|
| `Gemstone` | 24,363 | UniFFI 生成的绑定 + xcframework 封装 |
| `Primitives` | 14,151 | TypeShare 生成的数据模型（`Chain.swift` / `ChainEvm.swift` 等在 `Sources/Generated/`） |
| `Localization` | 1,896 | SwiftGen 生成的文案访问器 |

**基础设施层**：

| 包 | 行数 | 职责 |
|---|---:|---|
| `Store` | 9,950 | 本地数据库（GRDB）：表、查询、迁移 |
| `GemAPI` | 1,768 | 后端 API 客户端 |
| `SwiftHTTPClient` | 869 | HTTP 客户端 |
| `NativeProviderService` | 237 | **实现 Rust 侧的 `AlienProvider` 回调**——Rust 要发网络请求时经由这里 |
| `Preferences` | 1,554 | 偏好设置存储 |
| `Keychain` | 1,891 | iOS Keychain 封装 |
| `Keystore` | 1,775 | 钱包密钥管理（对接 Core 的 keystore） |
| `Signer` | 145 | 签名调用的薄封装（真正的签名在 Rust） |

**领域层**：

| 包 | 行数 | 职责 |
|---|---:|---|
| `PrimitivesComponents` | 12,609 | 领域相关的可复用视图（`ChainImage.swift` 等在 `Sources/Types/`） |
| `FeatureServices` | 10,387 | 各业务域的 service |
| `GemstonePrimitives` | 4,273 | Gemstone 类型的 Swift 侧扩展 |
| `ChainServices` | 1,979 | 链相关 service |
| `Blockchain` | 719 | 链操作封装 |
| `SystemServices` | 380 | 系统能力 service |

**通用层**：

| 包 | 行数 | 职责 |
|---|---:|---|
| `Components` | 6,389 | 与领域无关的通用 UI 组件 |
| `Style` | 1,925 | 设计系统：颜色、字体、图片资源（`Sources/Resources/Assets.xcassets/chains/` 放链图标） |
| `Formatters` | 2,097 | 格式化（金额、日期、地址） |
| `Validators` | 1,110 | 输入校验 |

### 4.2 Features/ —— 23 个业务域

`Assets` · `Contacts` · `EventPresenterService` · `FiatConnect` · `InAppNotifications` · `InfoSheet` · `LockManager` · `ManageWallets` · `MarketInsight` · `NFT` · `Onboarding` · `Perpetuals` · `PriceAlerts` · `QRScanner` · `Recents` · `Settings` · `Stake` · `Support` · `Swap` · `Transactions` · `Transfer` · `WalletConnector` · `WalletTab`

每个 Feature 内部是 SwiftUI View + MVVM ViewModel，service 从 `Packages/FeatureServices` 注入。

### 4.3 Gem/ —— 宿主 App

| 子目录 | 职责 |
|---|---|
| `App.swift` | 入口 |
| `Navigation/` | 全局导航 |
| `Scenes/` | 场景组装 |
| `Services/` | App 级服务 |
| `ViewModels/` · `Views/` · `Types/` | App 级实现 |
| `Resources/` | 资源 |

---

## 5. android/ —— Compose 应用

约 120 个 Gradle 模块，分五层。

### 5.1 模块分层

| 模块 | 职责 |
|---|---|
| `:app` | 应用入口、DI 装配（Hilt）、导航 |
| `:gemstone` | UniFFI 绑定 + `libgemstone.so`（Gradle 自动生成） |
| `:gemcore` | **TypeShare 生成的模型**（`com/wallet/core/primitives/generated/`）+ 领域扩展（`ext/` / `domains/`） |
| `:blockchain` | 链操作封装、账户创建 operator |
| `:ui` | 通用 Compose 组件 + **生成的 `strings.xml`**（`src/main/res/`） |
| `:ui-models` | UI 层数据模型 |
| `:data` | 数据层根 |
| `:data:repositories` | 仓储实现 |
| `:data:services` | 服务层 |
| `:data:services:store` | 本地数据库 |
| `:data:services:remote-gem` | 后端 API 客户端 |
| `:data:services:walletconnect:reown` | WalletConnect 实现（Reown SDK） |
| `:data:services:walletconnect:noop` | WalletConnect 空实现（不含 SDK 的构建变体用） |
| `:data:coordinators` | 跨服务协调 |
| `:flavors:fcm` | Firebase 推送实现 |
| `:flavors:pushes-stub` | 推送空实现（无 GMS 变体） |
| `:flavors:google-review` | Google Play 应用内评价 |
| `:flavors:review-stub` | 评价空实现 |

> `noop` / `stub` 这组模块是为了产出**不含 Google 服务的构建变体**——需要替换实现时，照这个模式加，不要在业务代码里写 `if (hasGms)`。

### 5.2 features/ —— 23 个业务域，每域两个模块

```
:features:<域>:presents      Compose UI
:features:<域>:viewmodels    ViewModel
```

业务域：`activities` · `add_asset` · `asset` · `asset_select` · `assets` · `banner` · `bridge` · `buy` · `confirm` · `create_wallet` · `earn`（含 `delegation` / `stake` 两个子域）· `import_wallet` · `nft` · `perpetual` · `receive` · `recipient` · `referral` · `settings`（含 8 个子域）· `swap` · `transfer_amount` · `update_app` · `wallet-details` · `wallets`

`:features:settings` 的子域：`aboutus` · `contacts` · `currency` · `develop` · `in_app_notifications` · `networks` · `price_alerts` · `security` · `settings`

**presents / viewmodels 拆分是硬约定**，加新 feature 照这个结构建两个模块。

### 5.3 其他

| 目录 | 职责 |
|---|---|
| `google/` | Google 服务相关配置 |
| `fastlane/` | 发布自动化 |
| `reproducible/` | 可复现构建 |
| `gradle/` | 版本目录与 wrapper |

---

## 6. 其余顶层目录

### 6.1 localization/

**移动端文案的唯一源头**，Fluent 格式，消息 id 用下划线（如 `common_cancel`）。

| 目录 | 用途 | 生成到 |
|---|---|---|
| `localization/app/*.ftl` | App 内文案 | `ios/Packages/Localization/` + `android/ui/src/main/res/` |
| `localization/InfoPlist/*.ftl` | iOS 系统权限描述 | iOS InfoPlist |
| `localization/widget/*.ftl` | iOS 小组件文案 | iOS widget |

改完跑 `just localize`。**生成的 `.strings` / `strings.xml` / SwiftGen 文件不要手改。**

新增 key 时：按前缀放进对应分区（`common_*` 放 `# Common`），**每个语言文件都要加**，不要留缺失让生成过程掩盖。

> Core 侧文案是另一套，在 `core/crates/localizer/i18n/`，两者不要混。

### 6.2 maestro/

Maestro UI 自动化流程（目前只有 `android/`）。什么时候该用 Maestro、什么时候该用单元测试或原生 UI 测试，见 [skills/testing-maestro.md](skills/testing-maestro.md)。

### 6.3 scripts/

发布链路：`bump.sh`（版本号）· `release.sh` · `rollback.sh` · `verify-release.sh` · `preflight.sh` · `package-xcframework.sh`。配置在 `release.env`（模板 `release.env.example`）。

### 6.4 build/

`GemstoneFFI.xcframework` —— iOS 链接的 Rust 静态库产物，由 `package-xcframework.sh` 生成。

### 6.5 docs/ 与 skills/

| 目录 | 内容 |
|---|---|
| `docs/` | 跨端子系统设计：[DEEPLINKS.md](docs/DEEPLINKS.md) · [PAYMENTS.md](docs/PAYMENTS.md) · [DEVICE_SUBSCRIPTIONS.md](docs/DEVICE_SUBSCRIPTIONS.md) · [SWAPPER.md](docs/SWAPPER.md) |
| `core/docs/` | Core 子系统：Keystore v4、设备认证、钱包认证、WebSocket、积分、[FEATURES.md](core/docs/FEATURES.md)（**链能力矩阵，接链后必须更新**） |
| `skills/` | 仓库规范：架构、安全、代码审查、发布流程、质量检查、本地化、新功能流程、决策记录 |
| `ios/skills/` · `android/skills/` · `core/skills/` | 各平台的专属规范 |

---

## 7. 依赖流向

```
                    primitives          ← 根依赖，几乎所有 crate 都 import
                        ▲
        ┌───────────────┼───────────────┐
        │               │               │
   chain_traits    gem_client      serde_serializers
        ▲               ▲
        │               │
   gem_* 链实现  ←  gem_jsonrpc
        ▲
        ├──────────────────────────┐
        │                          │
   settings_chain              gemstone/
   （后端装配：配置+索引器）      （移动端装配：RPC-only）
        ▲                          ▲
        │                          │
   apps/（api/daemon/           ios/ + android/
   dynode/egress）
        │
   storage/ + cacher/ + streamer/
```

两条关键分岔：

| | 后端路径 | 移动端路径 |
|---|---|---|
| 链 provider 装配 | `settings_chain`（含第三方索引器） | `gemstone/gateway/chain_factory.rs`（RPC-only） |
| HTTP 实现 | `gem_client::ReqwestClient` | `gem_client::AlienProvider`（委托给平台） |
| 数据库 | `storage`（PostgreSQL + Diesel） | iOS `Store`（GRDB）/ Android `:data:services:store` |
| 文案 | `crates/localizer` | `localization/*.ftl` → 双端生成物 |

**`primitives` 是根依赖，改它会波及所有东西。** `gem_client` 的双实现是移动端"网络请求委托给平台"这一设计的落点。

---

## 8. 速查：我要改 X，该去哪

| 我要… | 去这里 |
|---|---|
| 加一条新链 | 见[接链文档](如何添加新链（包括EVM兼容）.md)。核心是 `primitives/chain_config.rs` + 新建 `gem_*` crate（非 EVM） |
| 改链的属性（出块时间、chain id、是否支持 swap） | `core/crates/primitives/src/chain_config.rs` —— 集中在一处，不散落 |
| 加一个领域模型给双端用 | `core/crates/primitives/`，打 TypeShare 标注，跑 `just generate` |
| 给双端加一个可调用的函数 | `core/gemstone/src/`，用 UniFFI 导出 |
| 改余额 / 交易记录 / 代币元数据的取法 | 对应 `gem_*` crate 的 `provider/` 目录 |
| 改交易记录或代币发现的数据源 | `gem_evm/src/rpc/indexer/`（EVM）或对应链 crate 的 `rpc/indexer/` |
| 改签名 | 对应 `gem_*` crate 的 `signer/`；算法层在 `crates/signer/`。**高风险，先读 [skills/security.md](skills/security.md)** |
| 改助记词派生 | `core/crates/gem_derivation/`。**高风险** |
| 改价格来源 | `core/crates/prices/providers/` |
| 加一个 swap provider | `core/crates/swapper/`，照 [core/skills/swapper-checklist.md](core/skills/swapper-checklist.md) |
| 改 App 里的一句文案 | `localization/app/*.ftl`（**所有语言都要加**），然后 `just localize` |
| 改 Core 里的一句文案 | `core/crates/localizer/i18n/<lang>/localizer.ftl` |
| 改后端数据库表 | `core/crates/storage/`（Diesel schema + migration） |
| 改 RPC 节点配置 | `core/Settings.yaml` + `core/apps/dynode/chains.yml` |
| 加 iOS 界面 | `ios/Features/<域>/`，组件从 `Packages/Components` 或 `PrimitivesComponents` 取 |
| 加 Android 界面 | `android/features/<域>/presents` + `viewmodels`，两个模块都要建 |
| 加链图标 | iOS：`ios/Packages/Style/Sources/Resources/Assets.xcassets/chains/`；Android 从生成的枚举解析 |
| 改发布流程 | `scripts/`，规范见 [skills/release-process.md](skills/release-process.md) |
| 查某条链支持哪些能力 | [core/docs/FEATURES.md](core/docs/FEATURES.md) |

---

## 附录 A：规模排行

**Core crates 前 15**：

| crate | 行数 |
|---|---:|
| `swapper` | 26,369 |
| `primitives` | 20,268 |
| `gem_evm` | 13,489 |
| `storage` | 11,195 |
| `gem_hypercore` | 8,032 |
| `gem_sui` | 8,009 |
| `gem_tron` | 7,126 |
| `fiat` | 7,053 |
| `gem_solana` | 6,114 |
| `gem_ton` | 5,104 |
| `gem_bitcoin` | 3,818 |
| `gem_aptos` | 3,245 |
| `gem_cosmos` | 3,121 |
| `gem_near` | 2,756 |
| `gem_xrp` | 2,478 |

**后端服务**：`daemon` 10,949 · `dynode` 6,266 · `api` 6,122 · `egress` 1,523

**iOS 包前 6**：`Gemstone` 24,363（生成）· `Primitives` 14,151（生成）· `PrimitivesComponents` 12,609 · `FeatureServices` 10,387 · `Store` 9,950 · `Components` 6,389

## 附录 B：与已有文档的差异

[core/skills/project-structure.md](core/skills/project-structure.md) 写于较早版本，以下几处与当前代码不符——读那份文档时注意：

| 它写的 | 实际情况 |
|---|---|
| `prices_dex/` | 不存在，现在是 `prices/`（含 DEX provider） |
| `nft_client/` · `nft_provider/` | 不存在，已合并进 `nft/` |
| "50+ crates" | 实际 65 个 |
| 未提及 | `defi` · `yielder` · `portfolio` · `lists` · `simulation` · `support` · `gem_auth` · `gem_crypto` · `gem_derivation` · `gem_encoding` · `gem_keystore` · `gem_rewards` · `gem_wallet_connect` · `gem_alchemy` · `gem_ankr` · `gem_blockscout` · `gem_optimism` · `gem_tempo` · `gem_bsc` · `in_app_notifications` · `metrics` · `coinmarketcap` · `jupiter` |
| `apps/` 只列 3 个 | 实际 4 个，多一个 `egress`（出站 API 网关） |
| `bin/` 只列 4 个 | 实际 6 个，多 `cli` 和 `nodecheck` |

本文以实测为准。两份文档冲突时，按仓库规范"选更新或更可验证的那个，并标注另一个待清理"——本附录即是那个标注。
